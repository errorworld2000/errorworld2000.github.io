<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>gameframework细节实现</title>
    <url>/gameframework/gameframework%E7%BB%86%E8%8A%82%E5%AE%9E%E7%8E%B0/</url>
    <content><![CDATA[Reference
IReference
被引用的对象需实现 IReference 接口（包含一个 Clear 函数），当对象被回收时调用，恢复初始状态，以便下次使用。
ReferenceCollection
代表一种类型的引用池，位于 ReferencePool 内部：
private readonly Queue&lt;IReference&gt; m_References;private readonly Type m_ReferenceType;// ...状态计数````### ReferenceInfo获取引用池的状态计数，相较 `ReferenceCollection` 少了 `m_References`。### ReferencePool静态类，管理所有类型的引用池：```csharpprivate static readonly Dictionary&lt;Type, ReferenceCollection&gt; s_ReferenceCollections = new Dictionary&lt;Type, ReferenceCollection&gt;();public static T Acquire&lt;T&gt;() where T : class, IReference, new() =&gt; GetReferenceCollection(typeof(T)).Acquire&lt;T&gt;();public static IReference Acquire(Type referenceType)&#123;    InternalCheckReferenceType(referenceType);    return GetReferenceCollection(referenceType).Acquire();&#125;

具体操作时先根据类型（为 Type 时先要对类型进行检查）获取对应的 ReferenceCollection，再进行其他操作。


Fsm (状态机)
FsmState
状态机状态的抽象类，包括状态初始化、状态更新等逻辑。
IFsm
状态机接口。
FsmBase
状态机的抽象类。
Fsm
internal sealed class Fsm&lt;T&gt; : FsmBase, IReference, IFsm&lt;T&gt; where T : class&#123;    private T m_Owner;    private readonly Dictionary&lt;Type, FsmState&lt;T&gt;&gt; m_States;    private Dictionary&lt;string, Variable&gt; m_Datas;    private FsmState&lt;T&gt; m_CurrentState;&#125;

一个状态机，包括所有状态类以及当前数据。

IFsmManager
接口，管理所有状态机。

Procedure
ProcedureManager
特殊的 Fsm，也由 FsmManager 管理。

Setting
只需根据 ISettingHelper 创建设置辅助器即可完成，ISettingManager 用于使用。

Module
只暴露接口，具体实现仅在程序集可见。通过 GetModule&lt;Interface&gt; 实例化：
public static bool InScene(this GameObject gameObject)

this：表示这是一个扩展方法，并且该方法是对 GameObject 类型的扩展。
GameObject：表示要扩展的类型（Unity 的 GameObject 类）。
gameObject：方法参数，调用时传入的目标对象。


DataProvider
ReadData
m_ResourceManager.LoadAsset 加载资产，并传入回调函数，回调函数触发 provider 的事件：
private void OnCloseUIFormComplete(object sender, CloseUIFormCompleteEventArgs e)&#123;    m_EventComponent.Fire(this, CloseUIFormCompleteEventArgs.Create(e));&#125;m_UIManager.CloseUIFormComplete += OnCloseUIFormComplete;

在 GameFramework 提供接口，在 runtime 中将不同模块结合起来。


]]></content>
      <categories>
        <category>gameframework</category>
      </categories>
      <tags>
        <tag>c#</tag>
        <tag>unity</tag>
      </tags>
  </entry>
  <entry>
    <title>Unity协程深入分析</title>
    <url>/gameframework/unity%E5%8D%8F%E7%A8%8B/</url>
    <content><![CDATA[1. 协程 vs Update 轮询

Update轮询：每帧主动检测状态，需要手动管理状态逻辑。
协程 Coroutine：逻辑暂停，等待条件满足时 Unity 自动恢复执行。

核心区别对比



对比项
Update轮询
协程 Coroutine




检查方式
每帧自己 if 检查
yield return 后自动挂起


CPU开销
多对象轮询累积大
集中管理，开销小


写法复杂度
高，状态机逻辑复杂
低，顺序逻辑直观


可读性
差，容易出错
像同步代码，自然清晰


出错概率
高，容易漏判或重复判定
低，写完即能跑




一句话总结：协程让“等待”自动化，无需每帧检查。


2. Update 检测请求示例（传统方法）
private UnityWebRequest m_Request;public void StartRequest(string url)&#123;    m_Request = UnityWebRequest.Get(url);    m_Request.SendWebRequest();&#125;private void Update()&#123;    if (m_Request == null || !m_Request.isDone) return;    if (m_Request.result == UnityWebRequest.Result.ConnectionError ||        m_Request.result == UnityWebRequest.Result.ProtocolError)    &#123;        Debug.LogError(&quot;Request Error: &quot; + m_Request.error);    &#125;    else    &#123;        Debug.Log(&quot;Request Success: &quot; + m_Request.downloadHandler.text);    &#125;    m_Request = null;&#125;

缺点：Update 负担重，需要手动管理状态。


3. 协程检测请求示例（推荐方法）
public void StartRequest(string url)&#123;    StartCoroutine(RequestCoroutine(url));&#125;private IEnumerator RequestCoroutine(string url)&#123;    using (UnityWebRequest request = UnityWebRequest.Get(url))    &#123;        yield return request.SendWebRequest();        if (request.result == UnityWebRequest.Result.ConnectionError ||            request.result == UnityWebRequest.Result.ProtocolError)        &#123;            Debug.LogError(&quot;Request Error: &quot; + request.error);        &#125;        else        &#123;            Debug.Log(&quot;Request Success: &quot; + request.downloadHandler.text);        &#125;    &#125;&#125;

优点：自动挂起等待，不用管理完成时机，逻辑自然顺序。


4. 协程使用场景总结

等待 I/O 操作：网络请求、文件读取、WWW 请求等。
动画控制：顺序播放动画或控制延迟。
条件等待：等待某些状态触发，例如玩家进入区域或敌人死亡。
循环任务：无需每帧 Update 手动判断。


5. 协程性能与调度机制

yield return 后，协程被挂起。
Unity 集中管理所有挂起的协程。
每帧只需检查协程状态量，不会像多对象轮询消耗过多 CPU。
条件满足或 I/O 完成后直接唤醒，继续执行剩余逻辑。
可以通过 StopCoroutine 或 StopAllCoroutines 控制协程生命周期。


6. 协程注意事项

不可用于精确时间控制：帧率不稳定会影响时间判断。
避免长期阻塞：协程内部最好不要进行 CPU 密集型计算。
嵌套协程管理：yield return StartCoroutine(...) 会等待子协程完成。
异常处理：协程内部异常不会自动抛出到主线程，需要 try-catch。


]]></content>
      <categories>
        <category>gameframework</category>
      </categories>
      <tags>
        <tag>c#</tag>
        <tag>unity</tag>
        <tag>coroutine</tag>
      </tags>
  </entry>
  <entry>
    <title>缓存系统</title>
    <url>/leveldb/cache/</url>
    <content><![CDATA[缓存系统
基于 LRUCache 的缓存系统，其结构包括哈希表和 LRU 双向链表。
LRUHandle

哈希表相关: next_hash 用于链接哈希表中的条目。
链表相关: next 和 prev 用于在 LRU 链表和使用中的链表（in_use）中形成双向链表。

哈希表


结构: 哈希表由多个存储桶组成，每个存储桶包含一个链接链表，用于存储散列到该存储桶中的缓存条目。


插入逻辑：

先找到对应的存储桶。
比较哈希值和键值，如果找到则替换条目，否则将新条目插入到链表末尾。
当条目数量超过容量时，重新调整哈希表的大小。



LRUCache
由哈希表、LRU 链表头和 in_use 链表头构成。

插入过程:

加锁。
根据键创建 handle，引用计数加 1，并插入到 in_use_ 链表。
将 handle 添加到哈希表中。
如果键已存在，则返回旧的 handle，并从缓存和链表中删除旧条目，引用计数减 1。
当缓存容量超出限制且 LRU 链表不为空时，从 LRU 链表中移除第一个条目，并从缓存和链表中删除。


查找过程:

加锁。
在哈希表中查找 handle 并返回，引用计数加 1。


释放过程:

使用完 handle 后需要手动释放。
加锁，引用计数减 1。


擦除过程:

加锁，从缓存中移除条目，引用计数减 1。



SharedLRUCache
对 LRUCache 进行进一步封装，通过取哈希值的高 4 位来决定分片位置。

分片优势: 通过分片将缓存操作分散到多个 LRUCache 实例中，提升了访问效率，降低了锁的竞争，改善了并发性能。

]]></content>
      <categories>
        <category>leveldb</category>
      </categories>
      <tags>
        <tag>c++</tag>
        <tag>dataset</tag>
      </tags>
  </entry>
  <entry>
    <title>迭代器</title>
    <url>/leveldb/dbiter/</url>
    <content><![CDATA[Iterator* DBImpl::NewIterator(const ReadOptions&amp; options) &#123;  SequenceNumber latest_snapshot;  uint32_t seed;  Iterator* iter = NewInternalIterator(options, &amp;latest_snapshot, &amp;seed);  return NewDBIterator(this, user_comparator(), iter,                       (options.snapshot != nullptr                            ? static_cast&lt;const SnapshotImpl*&gt;(options.snapshot)                                  -&gt;sequence_number()                            : latest_snapshot),                       seed);&#125;Iterator* DBImpl::NewInternalIterator(const ReadOptions&amp; options,                                      SequenceNumber* latest_snapshot,                                      uint32_t* seed) &#123;  mutex_.Lock();  *latest_snapshot = versions_-&gt;LastSequence();  // Collect together all needed child iterators  std::vector&lt;Iterator*&gt; list;  list.push_back(mem_-&gt;NewIterator());  mem_-&gt;Ref();  if (imm_ != nullptr) &#123;    list.push_back(imm_-&gt;NewIterator());    imm_-&gt;Ref();  &#125;  versions_-&gt;current()-&gt;AddIterators(options, &amp;list);  Iterator* internal_iter = NewMergingIterator(&amp;internal_comparator_, &amp;list[0],                                               static_cast&lt;int&gt;(list.size()));  versions_-&gt;current()-&gt;Ref();  IterState* cleanup = new IterState(&amp;mutex_, mem_, imm_, versions_-&gt;current());  internal_iter-&gt;RegisterCleanup(CleanupIteratorState, cleanup, nullptr);  *seed = ++seed_;  mutex_.Unlock();  return internal_iter;&#125;
具体代码解释如下
void DBIter::Next() &#123;  assert(valid_);  if (direction_ == kReverse) &#123;  // Switch directions?    direction_ = kForward;    // iter_ is pointing just before the entries for this-&gt;key(),    // so advance into the range of entries for this-&gt;key() and then    // use the normal skipping code below.    if (!iter_-&gt;Valid()) &#123;      iter_-&gt;SeekToFirst();    &#125; else &#123;      iter_-&gt;Next();    &#125;    if (!iter_-&gt;Valid()) &#123;      valid_ = false;      saved_key_.clear();      return;    &#125;    // saved_key_ already contains the key to skip past.  &#125; else &#123;    // Store in saved_key_ the current key so we skip it below.    SaveKey(ExtractUserKey(iter_-&gt;key()), &amp;saved_key_);    // iter_ is pointing to current key. We can now safely move to the next to    // avoid checking current key.    iter_-&gt;Next();    if (!iter_-&gt;Valid()) &#123;      valid_ = false;      saved_key_.clear();      return;    &#125;  &#125;  FindNextUserEntry(true, &amp;saved_key_);&#125;void DBIter::FindNextUserEntry(bool skipping, std::string* skip) &#123;  // Loop until we hit an acceptable entry to yield  assert(iter_-&gt;Valid());  assert(direction_ == kForward);  do &#123;    ParsedInternalKey ikey;	    if (ParseKey(&amp;ikey) &amp;&amp; ikey.sequence &lt;= sequence_) &#123;	// 根据当前iter_的key封装成一个新ikey，同时要求ikey的序列号小于创建DBIter时传入的序列号      switch (ikey.type) &#123;        case kTypeDeletion:          // Arrange to skip all upcoming entries for this key since          // they are hidden by this deletion.          SaveKey(ikey.user_key, skip);		          skipping = true;		// 如果当前ikey的类型是删除，将跳过整个 user_key = ikey.user_key()的nodes          break;        case kTypeValue:          if (skipping &amp;&amp;	// 跳过              user_comparator_-&gt;Compare(ikey.user_key, *skip) &lt;= 0) &#123;	// 旧条目不再需要            // Entry hidden          &#125; else &#123;	// 否则就是本 user_key 所在nodes中的最新节点            valid_ = true;            saved_key_.clear();            return;          &#125;          break;      &#125;    &#125;    iter_-&gt;Next();	// 定位到下一个  &#125; while (iter_-&gt;Valid());  saved_key_.clear();  valid_ = false;&#125;
1. DBIter::Next()


作用：
实现迭代器前进到下一个用户可见的条目。


核心逻辑：


断言当前状态有效：
assert(valid_); 确保调用 Next() 时，迭代器处于有效状态。


处理不同方向：


如果当前迭代方向为 kReverse，说明之前的操作是反向遍历：
切换方向为 kForward。因为反向遍历时迭代器指向当前 key 之前的位置，所以需要先移动一步使其进入当前 key 的区间：若当前迭代器无效，则调用 SeekToFirst() 重置到首元素。否则调用 iter_-&gt;Next()。若移动后迭代器变为无效，则标记迭代器为无效并清空 saved_key_，返回。此时，saved_key_ 内保存了需要跳过的 key。


当方向已经是kForward时：
首先将当前 key 提取后保存到 saved_key_，用于后续跳过同一用户键的所有条目（防止重复返回）。调用 iter_-&gt;Next() 移动到下一个内部条目。同样，若移动后无效，则更新状态并返回。




寻找下一个用户条目：
最后调用 FindNextUserEntry(true, &amp;saved_key_)，从当前位置继续寻找一个用户可见的有效条目。





2. DBIter::FindNextUserEntry(bool skipping, std::string* skip)


作用：
在内部迭代器当前位置开始，寻找下一个用户层面上“有效”（即未被删除、未被隐藏）的条目。


核心逻辑：


循环遍历：
循环直到找到一个符合条件的用户条目，或者迭代器到达末尾。


解析内部键：
每次循环调用 ParseKey(&amp;ikey) 解析当前内部键 iter_-&gt;key()，同时检查 ikey.sequence 是否在允许的序列号范围内（&lt;= sequence_）。


根据操作类型处理：


删除类型（kTypeDeletion）：
将当前 ikey.user_key 保存到 skip（即 saved_key_），并设置 skipping 为 true，表示后续相同用户键的条目应当被跳过（删除操作“屏蔽”了旧值）。


值类型（kTypeValue）：
如果处于跳过状态（skipping 为 true），并且当前 ikey.user_key 与 *skip 比较小于等于（即还处于需要跳过的范围内），则认为该条目被删除或隐藏，不予返回。否则，找到了一个有效的用户条目，将 valid_ 标记为 true，清空 saved_key_（因为找到有效数据后，不需要再跳过）。返回，结束循环。




结束条件：

如果循环结束时 iter_-&gt;Valid() 为 false，则表示已经遍历完所有条目，
清空 saved_key_，并将 valid_ 标记为 false。





]]></content>
      <categories>
        <category>leveldb</category>
      </categories>
      <tags>
        <tag>c++</tag>
        <tag>dataset</tag>
      </tags>
  </entry>
  <entry>
    <title>日志</title>
    <url>/leveldb/log/</url>
    <content><![CDATA[日志结构
.pskdljedkydu{zoom:67%;}

static const int kBlockSize = 32768;
// Header is checksum (4 bytes), length (2 bytes), type (1 byte).
static const int kHeaderSize = 4 + 2 + 1

日志写入流程

初始化写入器
调用 Next 函数获取 singleWriter，用于处理单条日志记录的写入。
分块（Chunk）管理

开始首个 Chunk：singleWriter 初始化后，启动第一个 Chunk 的写入。
数据缓冲：将日志记录数据按顺序写入内存缓冲区。
缓冲区阈值检测：持续监测缓冲区大小，当数据部分（不含 Header）达到 32 KiB - 7 字节（预留 7 字节 Header 空间）时，触发分块操作。


构建并写入 Chunk

生成 Header：为当前 Chunk 计算校验和，并确定类型（如跨块时的 First/Middle，或结尾的 Last）。
完整 Chunk 写入：将 Header（7 字节） + 数据（≤32 KiB -7 字节） 组合成完整 Chunk，一次性写入日志文件。
重置缓冲区：清空当前缓冲区，准备接收后续数据。


循环处理与结束标记

重复上述过程，直至日志记录所有数据写入完成。
最后一个 Chunk 标记为 Last 类型，表示记录结束。若数据恰好填满 Chunk，则前一个 Chunk 标记为 Last。




日志读取流程

按块读取数据
每次从日志文件读取 32 KiB 的 Block，作为基础处理单元。
初始化读取器
调用 Next 函数获取 singleReader，用于逐条解析日志记录。
解析 Chunk

读取 Header：从 Block 头部提取 校验和、数据长度、类型（如 Full/First/Middle/Last）。
数据提取：根据 Header 中的数据长度，从 Block 中读取对应字节的数据内容。


校验与错误处理

校验和验证：计算数据部分的校验和，与 Header 中的值比对。
错误处理：

校验失败：若用户启用严格模式，返回错误；否则丢弃当前 Chunk，继续读取后续 Block。
校验成功：保留数据，准备拼接。




数据拼接与终止条件

中间 Chunk：若类型为 First 或 Middle，将数据暂存，继续读取下一 Block。
终止 Chunk：遇到 Last 类型时，将当前 Chunk 数据与前序数据合并，返回完整日志记录。



]]></content>
      <categories>
        <category>leveldb</category>
      </categories>
      <tags>
        <tag>c++</tag>
        <tag>dataset</tag>
      </tags>
  </entry>
  <entry>
    <title>内存数据库</title>
    <url>/leveldb/memtable/</url>
    <content><![CDATA[
内存数据库
leveldb内存数据库基于跳表结构。
跳表
template &lt;typename Key, class Comparator&gt;class SkipList &#123;    private:    struct Node;&#125;template &lt;typename Key, class Comparator&gt;struct SkipList&lt;Key, Comparator&gt;::Node &#123;    explicit Node(const Key&amp; k) : key(k) &#123;&#125;        const Key key;        Node* Next(int n) &#123;        assert(n &gt;= 0);        return next_[n].load(std::memory_order_acquire);    &#125;        void SetNext(int n, Node* x) &#123;        assert(n &gt;= 0);        next_[n].store(x, std::memory_order_release);    &#125;        Node* NoBarrier_Next(int n) &#123;        assert(n &gt;= 0);        return next_[n].load(std::memory_order_relaxed);    &#125;        void NoBarrier_SetNext(int n, Node* x) &#123;        assert(n &gt;= 0);        next_[n].store(x, std::memory_order_relaxed);    &#125;private:    std::atomic&lt;Node*&gt; next_[1];&#125;;
memory_order 说明

memory_order_relaxed: 只确保操作是原子性的，不对内存顺序做任何保证。
memory_order_release: 用于写操作，例如 std::atomic::store(T, memory_order_release)，会在写操作之前插入一个 StoreStore屏障，确保屏障之前的所有操作不会重排到屏障之后。
memory_order_acquire: 用于读操作，例如 std::atomic::load(memory_order_acquire)，会在读操作之后插入一个 LoadLoad 屏障，确保屏障之后的所有操作不会重排到屏障之前。
memory_order_acq_rel: 等效于 memory_order_acquire 和 memory_order_release 的结合，同时插入一个 StoreStore 屏障与 LoadLoad 屏障，用于读写操作，例如 std::atomic::fetch_add(T, memory_order_acq_rel)。

示例
// 线程 A (产生者)node-&gt;data = 42;  // 初始化数据list.SetNext(0, node);  // 通过 memory_order_release 让数据对其他线程可见// 线程 B (消费者)Node* node = list.Next(0);  // 使用 memory_order_acquire 获取节点int value = node-&gt;data;  // 确保读取的是线程 A 完全初始化后的数据
编译器优化：编译器可能会认为，node-&gt;data = 42 和 list.SetNext(0, node) 没有直接依赖关系。因此，它可能将 list.SetNext(0, node) 提前。编译器这种优化是合法的，因为在单线程语境下，它认为不会改变行为。
模板例子
template &lt;typename T&gt;class Example &#123;public:    struct NestedType &#123;&#125;;     // 嵌套类型    static int value;         // 静态非类型成员&#125;;template &lt;typename T&gt;void Func() &#123;    typename Example&lt;T&gt;::NestedType* ptr;  // 嵌套类型    Example&lt;T&gt;::value = 42;       // 静态成员&#125;// 对于 Example&lt;T&gt;::NestedType，编译器可能无法区分它是一个类型还是非类型成员（如变量、函数等）。// 在模板类或函数中，如果某个嵌套成员依赖于模板参数，编译器无法在模板实体化之前确定它的具体含义。~~~c++template &lt;typename Key, class Comparator&gt;typename SkipList&lt;Key, Comparator&gt;::Node* SkipList&lt;Key, Comparator&gt;::NewNode(    const Key&amp; key, int height) &#123;  char* const node_memory = arena_-&gt;AllocateAligned(      sizeof(Node) +      sizeof(std::atomic&lt;Node*&gt;) * (height - 1));  // 动态内存分配Node*  return new (node_memory) Node(key);&#125;//t柔性数组和定制内存分配，优化了跳表的内存效率、访问速度
跳表的查找

根据跳表高度选取最高层的头节点；
若小于，取该层的下一个节点；
若等于，则直接返回；
若大于且层高不为0，降低层高继续查找。

跳表的插入

查找过程中，不断记录每层的前任节点；
为新节点随机生成层高，并根据前任节点信息，将节点插入到层的链表中；

graph TD    subgraph MemTable_System [&quot;MemTable 系统&quot;]        MemTable --&gt; SkipList        MemTable --&gt; Arena        SkipList --&gt; Entry        Entry --&gt; Arena        SkipList --&gt; KeyComparator    end
.ggnjgybajdqe{zoom:50%;}]]></content>
      <categories>
        <category>leveldb</category>
      </categories>
      <tags>
        <tag>c++</tag>
        <tag>dataset</tag>
      </tags>
  </entry>
  <entry>
    <title>读写过程</title>
    <url>/leveldb/read-write/</url>
    <content><![CDATA[写入过程
Leveldb的put/delete操作实际都被转成batch然后write。
WriteBatch batch;batch.Put(kvp.first, kvp.second);EXPECT_TRUE(db_-&gt;Write(WriteOptions(), &amp;batch).ok());
在leveldb中，使用Write操作进行写入，流程如下：

创建writer对象并加入队列（同时只允许一个写入操作运行）

Status DBImpl::Write(const WriteOptions&amp; options, WriteBatch* updates) &#123;  Writer w(&amp;mutex_);  w.batch = updates;  w.sync = options.sync;  w.done = false;  MutexLock l(&amp;mutex_);  writers_.push_back(&amp;w);  // 若当前 Writer 不是队列头部，则等待（锁被释放，进入阻塞）  while (!w.done &amp;&amp; &amp;w != writers_.front()) &#123;    w.cv.Wait();  &#125;  if (w.done) &#123;    return w.status;  &#125;  ...&#125;

为写操作分配空间

Status status = MakeRoomForWrite(updates == nullptr);uint64_t last_sequence = versions_-&gt;LastSequence();Writer* last_writer = &amp;w;

构建批次并执行写操作

if (status.ok() &amp;&amp; updates != nullptr) &#123;  WriteBatch* write_batch = BuildBatchGroup(&amp;last_writer);  WriteBatchInternal::SetSequence(write_batch, last_sequence + 1);  last_sequence += WriteBatchInternal::Count(write_batch);  &#123;    mutex_.Unlock();    status = log_-&gt;AddRecord(WriteBatchInternal::Contents(write_batch));    bool sync_error = false;    if (status.ok() &amp;&amp; options.sync) &#123;      status = logfile_-&gt;Sync();      if (!status.ok()) &#123;        sync_error = true;      &#125;    &#125;    if (status.ok()) &#123;      status = WriteBatchInternal::InsertInto(write_batch, mem_);    &#125;    mutex_.Lock();    if (sync_error) &#123;      RecordBackgroundError(status);    &#125;  &#125;  versions_-&gt;SetLastSequence(last_sequence);&#125;

清理并唤醒写操作

while (true) &#123;  Writer* ready = writers_.front();  writers_.pop_front();  if (ready != &amp;w) &#123;    ready-&gt;status = status;    ready-&gt;done = true;    ready-&gt;cv.Signal();  &#125;  if (ready == last_writer) break;&#125;if (!writers_.empty()) &#123;  writers_.front()-&gt;cv.Signal();&#125;
读取过程

加锁，基于snapshot进行数据读取

Status DBImpl::Get(const ReadOptions&amp; options, const Slice&amp; key,                   std::string* value) &#123;  Status s;  MutexLock l(&amp;mutex_);  SequenceNumber snapshot;  if (options.snapshot != nullptr) &#123;    snapshot =        static_cast&lt;const SnapshotImpl*&gt;(options.snapshot)-&gt;sequence_number();  &#125; else &#123;    snapshot = versions_-&gt;LastSequence();  &#125;    ......&#125;

引用mem、imm以及current version，避免读取过程中被销毁

MemTable* mem = mem_;  MemTable* imm = imm_;  Version* current = versions_-&gt;current();  mem-&gt;Ref();  if (imm != nullptr) imm-&gt;Ref();  current-&gt;Ref();

查询数据，释放锁避免占用cpu时间过长

// Unlock while reading from files and memtables  &#123;    mutex_.Unlock();    // First look in the memtable, then in the immutable memtable (if any).    LookupKey lkey(key, snapshot);    if (mem-&gt;Get(lkey, value, &amp;s)) &#123;      // Done    &#125; else if (imm != nullptr &amp;&amp; imm-&gt;Get(lkey, value, &amp;s)) &#123;      // Done    &#125; else &#123;      s = current-&gt;Get(options, lkey, value, &amp;stats);      have_stat_update = true;    &#125;    mutex_.Lock();  &#125;

压缩解引等操作

if (have_stat_update &amp;&amp; current-&gt;UpdateStats(stats)) &#123;    MaybeScheduleCompaction();  &#125;  mem-&gt;Unref();  if (imm != nullptr) imm-&gt;Unref();  current-&gt;Unref();  return s;

leveldb读取分为三步：
在memory db中查找指定的key，若搜索到符合条件的数据项，结束查找；
在冻结的memory db中查找指定的key，若搜索到符合条件的数据项，结束查找；
按低层至高层的顺序在level i层的sstable文件中查找指定的key，若搜索到符合条件的数据项，结束查找，否则返回Not Found错误，表示数据库中不存在指定的数据；
注解
注意leveldb在每一层sstable中查找数据时，都是按序依次查找sstable的。
0层的文件比较特殊。由于0层的文件中可能存在key重合的情况，因此在0层中，文件编号大的sstable优先查找。理由是文件编号较大的sstable中存储的总是最新的数据。
非0层文件，一层中所有文件之间的key不重合，因此leveldb可以借助sstable的元数据（一个文件中最小与最大的key值）进行快速定位，每一层只需要查找一个sstable文件的内容。

]]></content>
      <categories>
        <category>leveldb</category>
      </categories>
      <tags>
        <tag>c++</tag>
        <tag>dataset</tag>
      </tags>
  </entry>
  <entry>
    <title>排序字符串表</title>
    <url>/leveldb/sstable/</url>
    <content><![CDATA[
SSTable
物理结构
SSTable 按块划分，默认数据块大小为 4KiB。每个数据块除了存储数据外，还包括两个辅助字段：压缩类型和 CRC 校验码。
逻辑结构


Data Block：存储键值对。
Filter Block：存储过滤器相关数据，采用专门的 FilterBlock。
Meta Index Block：存储 Filter Block 在 SSTable 文件中的位置和长度。
Index Block：存储每个 Data Block 的索引信息。
Footer：存储 Meta Index Block 和 Index Block 的索引信息。

读写过程
写过程

读过程

SSTable 使用两种缓存机制：

Table Cache：存储已打开的 SSTable 文件。
Block Cache：存储已读取的数据块。

Table Cache 的结构如下：

Table 的迭代器
TwoLevelIterator
TwoLevelIterator 用于高效处理两层索引结构。它结合索引块和数据块，提供了一种高效的访问机制。
using BlockFunction = Iterator* (*)(void*, const ReadOptions&amp;, const Slice&amp;);explicit TwoLevelIterator(Iterator* index_iter, BlockFunction block_function,                          void* arg, const ReadOptions&amp; options);//使用 arg 而不是直接传递 Table* 主要是为了设计的灵活性和接口的一致性。
实现思路

索引块迭代器：遍历索引块的迭代器，用来获取每个数据块的元信息。
数据块读取函数：提供读取数据块的函数，该函数由 TwoLevelIterator 调用以加载具体的数据块。
封装 TwoLevelIterator：结合索引块迭代器和数据块读取函数，创建 TwoLevelIterator。


]]></content>
      <categories>
        <category>leveldb</category>
      </categories>
      <tags>
        <tag>c++</tag>
        <tag>dataset</tag>
      </tags>
  </entry>
  <entry>
    <title>整体架构</title>
    <url>/leveldb/%E6%95%B4%E4%BD%93%E6%9E%B6%E6%9E%84/</url>
    <content><![CDATA[Leveldb 整体架构
Leveldb 是一个高性能的键值存储库，由 Google 开发。它的设计目标是提供快速的读写操作，同时支持高效的压缩和持久化。以下是 Leveldb的核心组件及其工作原理的详细说明：

1. MemTable
概述

MemTable 是 Leveldb 在内存中用于组织和维护数据的结构。
写入操作不会直接刷新到磁盘文件，而是先写入 MemTable，因此写入性能非常高。

特点


基于 SkipList 实现，支持高效的插入、删除和查找操作。


数据按键排序存储，便于后续的持久化操作。


详情

详情见 内存数据库。


2. Immutable MemTable
概述

当 MemTable 的大小达到预设阈值时，它会转换为 Immutable MemTable。
Immutable MemTable 是不可修改的，只允许读取操作。

作用

后台压缩进程会将 Immutable MemTable 中的数据转换为 SSTable，并持久化到磁盘中。
这种设计避免了写入阻塞，同时保证了数据的持久化。


3. Log (Write-Ahead Log, WAL)
概述

Leveldb 的写入操作首先会顺序写入日志文件（Log），然后再写入 MemTable。
日志文件用于在系统崩溃时恢复未持久化的数据。

特点

日志文件是追加写入的，因此写入性能非常高。
日志文件与 MemTable 的数据保持一致，确保数据的可靠性。

详情

详情见 日志。


4. SSTable (Sorted String Table)
概述

当内存中的数据超过一定阈值时，Leveldb会将数据持久化到磁盘，形成 SSTable 文件。
SSTable 是 Leveldb在磁盘上存储数据的主要形式。

层级结构

SSTable 文件在逻辑上分为多个层级（Level），最底层为 Level 0，由内存直接持久化生成。
更高层级的 SSTable 文件通过 Compaction 操作整合生成，以减少文件之间的交集。

详情

详情见 排序字符串表。


5. Manifest
概述

Manifest 文件记录了 Leveldb的元数据信息，包括 SSTable 文件的元信息、版本变更等。

核心组件

FileMetaData：记录 SSTable 文件的元信息，包括文件编号、文件大小、最小键、最大键等。
VersionEdit：记录数据库版本的变更，可能包含新增的文件元信息、删除的文件等。

工作原理

Manifest 文件与 Write-Ahead Log (WAL) 使用相同的存储方式。
每次数据库启动时，都会通过 Manifest 文件恢复最新的版本信息。

详情

详情见。


6. 读写操作
概述

Leveldb的读写操作是其核心功能，设计目标是高效、可靠。

写操作

数据首先写入日志文件（Log），然后写入 MemTable。
当 MemTable 达到阈值时，转换为 Immutable MemTable，并触发后台压缩。

读操作

数据首先从 MemTable 和 Immutable MemTable 中查找。
如果未找到，则从磁盘中的 SSTable 文件中查找。

详情

详情见 读写过程。


7. 缓存系统
概述

Leveldb使用缓存系统来加速读取操作，减少磁盘 I/O。

核心组件

Block Cache：缓存 SSTable 文件中的数据块，加速读取操作。
Table Cache：缓存 SSTable 文件的元信息，减少文件打开和关闭的开销。

详情

详情见 缓存系统。


8. Compaction
概述

Compaction 是 Leveldb 的核心机制之一，用于整合和优化 SSTable 文件。

作用

减少 SSTable 文件之间的交集，提高读取效率。
清理过期或删除的数据，释放磁盘空间。

类型

Minor Compaction：将 Immutable MemTable 转换为 SSTable 文件。
Major Compaction：整合多个 SSTable 文件，生成更高层级的文件。

详情

详情见 。


总结
Leveldb的整体架构通过 MemTable、Immutable MemTable、Log、SSTable、Manifest 等组件的协同工作，实现了高效的读写操作和持久化存储。其设计充分考虑了性能、可靠性和可扩展性，适用于多种应用场景。
]]></content>
      <categories>
        <category>leveldb</category>
      </categories>
      <tags>
        <tag>c++</tag>
        <tag>dataset</tag>
      </tags>
  </entry>
  <entry>
    <title>Pimpl模式</title>
    <url>/other/Pimpl%E6%A8%A1%E5%BC%8F/</url>
    <content><![CDATA[核心思想
Pimpl 模式的主要目标是分离接口与实现，通过将类的实现细节封装在私有的实现类中，减少编译依赖并提高二进制兼容性。

示例代码
// my_class.hclass my_class &#123;   // 公共接口与保护成员private:   class impl;                 // 不完整类型声明   std::unique_ptr&lt;impl&gt; pimpl; // 指向私有实现类&#125;;// my_class.cppclass my_class::impl &#123;     // 私有数据和方法   // 可以随意修改而无需重新编译客户端&#125;;my_class::my_class() : pimpl(new impl) &#123;   // 初始化 impl 的值&#125;

优点


减少编译依赖

客户端只依赖类的接口（头文件），实现细节变化无需重新编译。



分离接口和实现

将类的私有数据和方法封装在 impl 中，接口保持不变。



提高可移植性和二进制兼容性

修改实现类不会破坏已经编译好的客户端代码。



隐藏实现细节

客户端无法直接访问实现类，保证封装性。




Pimpl vs 虚函数接口



特性
虚函数接口
Pimpl




二进制兼容性
修改虚函数表会破坏兼容性
修改 impl 不影响接口和客户端


封装性
公开继承，接口暴露实现
完全隐藏实现细节


编译依赖
接口和实现耦合，修改需要重新编译
客户端仅依赖头文件，不依赖实现




总结

Pimpl 模式是一种经典的 信息隐藏与接口分离 技术
适合库开发或需要保持二进制兼容性的场景
结合智能指针（如 unique_ptr）可安全管理资源


]]></content>
      <categories>
        <category>other</category>
      </categories>
      <tags>
        <tag>c++</tag>
        <tag>design pattern</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring基础</title>
    <url>/other/Spring%E5%9F%BA%E7%A1%80/</url>
    <content><![CDATA[一、Spring XML 配置与注解
1. 自动扫描 Bean (beans.xml)
&lt;context:component-scan base-package=&quot;com.example&quot;/&gt;


作用：扫描指定包及子包，将标注了以下注解的类注册为 Spring Bean：

@Component（普通组件）
@Service（业务逻辑层）
@Repository（DAO 层，支持异常转换）
@Controller（Web 控制层）



2. 常见注解说明



注解
说明




@Component
普通组件，交给 Spring 管理


@Service
业务逻辑组件，语义清晰


@Repository
DAO 层组件，支持异常转换


@Controller
控制层组件，处理 Web 请求


@Autowired
按类型自动注入依赖


@Qualifier(&quot;beanName&quot;)
与 @Autowired 配合，按名称注入


@PostConstruct
初始化方法（容器加载后执行）


@PreDestroy
销毁方法（容器关闭前执行）


@Scope(&quot;singleton&quot;)
单例（默认）


@Scope(&quot;prototype&quot;)
多例，每次获取新对象




二、Spring AOP 与 AspectJ 区别



特性
Spring AOP
AspectJ




实现方式
JDK 或 CGLIB 动态代理
编译时织入 / 类加载时织入


原理
运行时生成代理类，拦截方法调用插入增强
修改字节码，将切面代码插入目标类


优点
简单易用，非侵入式
性能高，控制精细


缺点
存在性能开销
配置复杂，侵入性强




三、项目结构分层（经典三层架构）
controller --&gt; service --&gt; dao     |            |          |   Web层       业务逻辑层   数据访问层

POJO：简单 Java 对象，表示数据结构
Controller：接收请求、返回响应
Service：封装业务逻辑
DAO：操作数据库 CRUD


四、MySQL 数据库设计范式



范式
核心要求
示例




1NF（第一范式）
字段原子性，每列不可拆分
姓名 不能为 &quot;张三,李四&quot;


2NF（第二范式）
消除部分依赖，非主键字段完全依赖主键
复合主键 学生ID+课程ID，姓名只依赖 学生ID 应拆表


3NF（第三范式）
消除传递依赖，非主键字段不依赖其他非主键字段
班级编号 → 班主任 → 电话号，电话号依赖班主任，应拆表




五、总结

Spring 支持 XML + 注解混合配置，提高开发灵活性
Spring AOP 简单易用；AspectJ 适合高性能场景
三层架构利于职责分离与项目维护
数据库设计应遵循范式，避免冗余和异常依赖


]]></content>
      <categories>
        <category>other</category>
      </categories>
      <tags>
        <tag>java</tag>
      </tags>
  </entry>
  <entry>
    <title>async</title>
    <url>/other/async/</url>
    <content><![CDATA[异步示例：制作早餐
static async Task Main(string[] args)&#123;    // 同步倒咖啡    Coffee cup = PourCoffee();    Console.WriteLine(&quot;coffee is ready&quot;);    // 并发煎蛋、炸薯饼、烤吐司    var eggsTask = FryEggsAsync(2);    var hashBrownTask = FryHashBrownsAsync(3);    var toastTask = MakeToastWithButterAndJamAsync(2);    var breakfastTasks = new List&lt;Task&gt; &#123; eggsTask, hashBrownTask, toastTask &#125;;    while (breakfastTasks.Count &gt; 0)    &#123;        // 等待任意一个任务完成        Task finishedTask = await Task.WhenAny(breakfastTasks);        if (finishedTask == eggsTask)            Console.WriteLine(&quot;eggs are ready&quot;);        else if (finishedTask == hashBrownTask)            Console.WriteLine(&quot;hash browns are ready&quot;);        else if (finishedTask == toastTask)            Console.WriteLine(&quot;toast is ready&quot;);        // 可选：处理异常        await finishedTask;        // 从任务列表移除已完成任务        breakfastTasks.Remove(finishedTask);    &#125;    // 同步倒橙汁    Juice oj = PourOJ();    Console.WriteLine(&quot;oj is ready&quot;);    Console.WriteLine(&quot;Breakfast is ready!&quot;);&#125;

🔹 I/O Bound（等待 I/O 的任务）


特点：程序瓶颈在等待外部资源（网络、磁盘等），CPU 大部分时间空闲。


解决方案：使用 await 异步等待，释放线程资源，避免阻塞主线程。


await 会做什么：

挂起当前方法（如 UI 回调）。
控制权交还给事件循环，不阻塞界面。
等待完成后再恢复执行。



示例：
var result = await HttpClient.GetAsync(&quot;https://api.example.com&quot;);

🔸 CPU Bound（计算密集型任务）

特点：瓶颈在本地 CPU 运算，I/O 很少。
解决方案：使用 Task.Run 将计算任务放到线程池，并行执行，减轻主线程压力。

示例：
var result = await Task.Run(() =&gt; CalculateSomethingHeavy());


参考文档：
Async in depth | Microsoft Docs


]]></content>
      <categories>
        <category>other</category>
      </categories>
      <tags>
        <tag>c#</tag>
      </tags>
  </entry>
  <entry>
    <title>gc</title>
    <url>/other/gc/</url>
    <content><![CDATA[为什么要有 GC（GC 出现的背景）


在 C/C++ 等语言中，内存分配（malloc/new）和释放（free/delete）需要手动管理。


手动管理常出错，例如：

内存泄漏：分配了但忘记释放。
悬空指针：释放了但还有引用指向那块内存。



GC（垃圾回收）使这类问题自动化处理，减轻程序员负担，但带来性能和暂停（stop-the-world）等代价。


GC 的几种核心算法 / 实现方法及其原理
下面是几种经典或主流 GC 算法的简要原理、优缺点对比：



算法
核心原理
优点
缺点




Mark-Sweep（标记-清除）
从“根”（root）开始标记所有可达对象 → 遍历堆，把未标记的对象清除。
不移动对象 → 指针引用简单；空间利用率还算不错（除碎片问题）
清扫阶段扫描整个堆；产生内存碎片；暂停时间长（必须停止应用线程）


复制算法（Copying / Semi-space / Cheney 的算法）
将堆分为两个半区：一边分配（from-space），满了之后将所有活对象复制到另一半（to-space），然后交换两边。
分配速度快；无需空闲列表管理；自动避免外部碎片；活对象移动后，新生代 gc 开销较低（年轻对象死亡率高）
需要两倍空间（两个半区）；复制活对象本身也有成本；指针更新复杂（因为对象被移动）


标记-压缩（Mark-Compact / 标记-整理）
先标记可达对象；然后将可达对象向堆的一端紧凑移动，更新所有指向这些对象的引用。
消除碎片；保持堆连续性，有利于缓存局部性；比传统标记-清除在碎片多时更稳定。
移动对象代价；必须更新所有指针引用；压缩阶段通常暂停时间较长。


分代 GC（Generational GC）
将对象分代（年轻代 / 老年代等），假设大多数对象很快死亡 → 年轻代频繁清理，用复制算法／较快垃圾回收；老年代较少清理，用标记-清除或压缩算法。
减少多数 GC 的暂停时间；年轻代 GC 快且频繁，整体开销低；效率高。
老年代回收仍然可能是高成本；跨代对象指针管理复杂；晋升策略调优不当可能导致老年代“存活”对象过多。




# 深度探索垃圾回收器(GC)
]]></content>
      <categories>
        <category>other</category>
      </categories>
      <tags>
        <tag>cs</tag>
      </tags>
  </entry>
  <entry>
    <title>git开发流程</title>
    <url>/other/git%E5%BC%80%E5%8F%91%E6%B5%81%E7%A8%8B/</url>
    <content><![CDATA[1. 分支核心职责



分支类型
生命周期
核心职责




master
永久
只读。指向最新已发布版本，生产环境代码最终来源。


release/x.y.z
长期
开发、测试、修复主战场。合并 feature 和 hotfix。新版本从上一个 release 创建。


feature/xxx
短期
开发新功能。从目标 release 创建，完成后合并回 release。


hotfix/xxx
短期、紧急修复
修复线上 Bug。从出问题的 release 创建，修复后合并回该分支并同步到后续 release 分支。




2. 个人开发标准流程
步骤 1：同步并创建分支
git checkout release/1.11.0         # 切换到目标 release 分支git pull origin release/1.11.0      # 拉取最新代码git checkout -b feature/your-task   # 创建功能分支
步骤 2：本地开发与提交
在 feature 分支上编码，并进行原子化提交：
git add .git commit -m &quot;feat: implement user login modal&quot;git add .git commit -m &quot;fix: resolve button style issue&quot;
步骤 3：整理提交记录（PR 前）
git fetch origingit rebase -i origin/release/1.11.0

合并零碎 commit，保持提交历史清晰。

步骤 4：推送并创建 PR
git push -f origin feature/your-task-name  # 强制推送

在 GitHub / GitLab 上，从功能分支向目标 release 发起 PR。


3. PR 审查与合并流程
第一步：代码审查

检查逻辑、规范、目标分支。
至少一名成员 Approve。

第二步：合并前检查

确认 CI/CD 全通过。
提交历史整理干净。
无合并冲突。

第三步：执行合并
推荐策略（严禁使用 Merge Commit）：



合并方式
操作方式
优点




Squash and Merge (推荐)
点击 Squash and Merge 按钮
将所有 commit 合并为一个，生成干净历史。


Rebase and Merge (可选)
点击 Rebase and Merge 按钮
保持线性历史，按顺序追加 commit。



第四步：清理分支

PR 合并后删除 feature 分支：

git push origin --delete feature/your-task-name

]]></content>
      <categories>
        <category>other</category>
      </categories>
      <tags>
        <tag>tool</tag>
      </tags>
  </entry>
  <entry>
    <title>neuronNet</title>
    <url>/other/neuronNet/</url>
    <content><![CDATA[Python神经网络代码总结 (Python Neural Network Code Summary)
这份代码是一个从零开始实现的、基础的前馈神经网络（Feedforward Neural Network）。它不依赖任何外部机器学习库，清晰地展示了神经网络的核心工作原理。

1. 核心概述 (Core Overview) 🧠
该代码通过三个核心类构建了一个简单的神经网络：

Neuron: 网络的基本计算单元。
NeuronLayer: 由多个神经元组成的层。
NeuronNet: 整合输入层、隐藏层和输出层，并负责网络的训练和预测。

其关键功能包括：

前向传播 (Feed Forward): 将输入数据从输入层传到输出层，得到预测结果。
反向传播 (Backpropagation): 计算预测误差，并将误差从输出层反向传播，用于更新神经元的权重（weights）和偏置（bias）。


2. 类结构与功能 (Class Structure &amp; Functionality)
Neuron 类
这是最基础的构建模块。

功能 (Functionality): 模拟单个神经元，负责接收输入、计算加权和、通过激活函数输出结果，并计算用于反向传播的梯度。
关键属性 (Key Attributes):

weights: 权重列表，决定了每个输入信号的重要性。
bias: 偏置项，用于调整神经元的激活阈值。


关键方法 (Key Methods):

calculate_output(inputs): 接收输入，计算加权和 (calculate_total_net_input)，并通过 Sigmoid 激活函数 (squash) 得到输出。
squash(total_net_input): 激活函数，公式为 f(x)=frac11+e−x。它将任意输入值压缩到 (0, 1) 区间。
pd_... 方法: “pd” 代表 “Partial Derivative” (偏导数)。这些方法是反向传播算法的核心，用于计算误差对网络中各个参数（权重、偏置）的梯度。例如 pd_error_wrt_total_net_output 计算的是误差对神经元输出值的偏导数。



NeuronLayer 类
神经元的集合。

功能 (Functionality): 管理一层中的所有神经元，并将输入数据传递给该层的所有神经元进行处理。
关键属性 (Key Attributes):

neurons: 一个包含多个 Neuron 对象的列表。


关键方法 (Key Methods):

feed_forward(inputs): 遍历层内所有神经元，收集它们的输出，并将输出列表作为该层的最终输出。



NeuronNet 类
完整的神经网络。

功能 (Functionality): 构建并管理整个网络（包含一个隐藏层和一个输出层）。它负责初始化权重、执行完整的前向传播，并通过训练数据来更新整个网络的参数。
关键属性 (Key Attributes):

hidden_layer: 隐藏层 (NeuronLayer 对象)。
output_layer: 输出层 (NeuronLayer 对象)。
learning_rate: 学习率，控制每次训练时权重和偏置的更新幅度。


关键方法 (Key Methods):

feed_forward(inputs): 驱动数据流过整个网络，从隐藏层到输出层。
train(training_inputs, training_outputs): 最核心的方法。它实现了完整的反向传播和梯度下降算法来学习。




3. 关键算法：train 方法解析 (Key Algorithm: train Method Explained)
train 方法是网络学习的关键，它严格遵循反向传播的步骤：

前向传播 (Forward Pass): 首先调用 feed_forward 计算当前网络对输入数据的预测输出。
计算输出层误差 (Calculate Output Layer Error):

计算每个输出层神经元的误差对“加权输入总和”的偏导数 (pd_error_wrt_output_neurons_total_net_input)。这是链式法则的第一步。


计算隐藏层误差 (Calculate Hidden Layer Error):

将输出层的误差反向传播回隐藏层。
对于每个隐藏层神经元，其误差是所有与之相连的输出层神经元误差的加权和。


更新权重和偏置 (Update Weights and Biases):

输出层更新: 利用步骤2计算的梯度，结合学习率，更新输出层中每个神经元的权重和偏置。
隐藏层更新: 利用步骤3计算的梯度，结合学习率，更新隐藏层中每个神经元的权重和偏置。
更新公式为：新权重 = 旧权重 - 学习率 * 误差梯度。



Python
# 伪代码：train方法的核心逻辑# Pseudocode: Core logic of the train method# 1. 计算输出层误差梯度output_layer_gradients = []for each neuron in output_layer:    gradient = calculate_error_gradient(target_output, neuron_output)    output_layer_gradients.append(gradient)# 2. 计算隐藏层误差梯度（反向传播）hidden_layer_gradients = []for each neuron in hidden_layer:    error = sum(output_layer_gradients * weights_connecting_to_it)    gradient = error * neuron_output * (1 - neuron_output) # Sigmoid导数    hidden_layer_gradients.append(gradient)# 3. 更新所有权重和偏置for each neuron in output_layer:    neuron.weights -= learning_rate * output_layer_gradient * hidden_layer_output    neuron.bias -= learning_rate * output_layer_gradientfor each neuron in hidden_layer:    neuron.weights -= learning_rate * hidden_layer_gradient * network_input    neuron.bias -= learning_rate * hidden_layer_gradient
1.先定义网络:写网络Net的Class，声明网络的实例net=Net()，
2.定义优化器optimizer=optim.xxx(net.parameters()，lr=xxx)，
3.再定义损失函数（自己写class或者直接用官方的，compute_loss=nn.MSELoss()或者其他。
4.在定义完之后，开始一次一次的循环：
①先清空优化器里的梯度信息，optimizer.zero_grad();

②再将input传入，output=net(input) ，正向传播

③算损失，loss=compute_loss(target,output)   ##这里target就是参考标准值GT，需要自己准备，和之前传入的input一一对应

④误差反向传播，loss.backward()

⑤更新参数，optimizer.step()[参考](https://wanghao15536870732.github.io/2019/08/07/%E7%A5%9E%E7%BB%8F%E7%BD%91%E7%BB%9C/)

]]></content>
      <categories>
        <category>other</category>
      </categories>
      <tags>
        <tag>ml</tag>
      </tags>
  </entry>
  <entry>
    <title>ow架构</title>
    <url>/other/ow%E6%9E%B6%E6%9E%84/</url>
    <content><![CDATA[核心架构


Overwatch 采用 ECS 架构 管理复杂度

Entity：ID + 一组组件（Component，仅存储状态，无行为）
System：执行行为，关注特定组件组合（tuple / component-slice），在这些组件上处理逻辑



优点：

状态与行为分离，System 更纯洁、可组合
组件只存储数据，行为集中在 System
大多数 System 纯函数式（只读组件），少数修改状态



单例组件（Singleton Component）

存储全局共享数据，例如输入状态（键盘、手柄等）
减少 System 间耦合



Utility 函数

封装共享行为（被多个 System 调用的小逻辑）
依赖少组件、低副作用，避免 System 之间互相调用




ECS 设计规范与权衡


组件无行为，System 尽量无状态


如果 System 需要状态 → 放在 Singleton Component


副作用控制：

Deferred Execution（推迟执行）：将副作用（如创建实体、特效）暂存到帧尾统一处理
防止 System 在帧中间互相依赖造成脏状态或竞态问题



实体创建与销毁

销毁推迟到帧末端安全
创建推迟可能导致同帧 System 无法获取新实体 → 需谨慎




网络同步 / Netcode
1. 预测与回滚（Prediction &amp; Reconciliation）

客户端响应用户输入立即做预测，不等服务器确认
使用固定帧率 / 命令帧（command frames）同步模拟
客户端保存输入历史和移动状态历史
收到服务器确认后回滚到最后确认帧，然后重放客户端输入

2. 输入滑动窗口 + 丢包补偿

每个发送包包含自上次确认以来的所有输入帧
服务器缓冲区接收输入并补偿丢失帧

3. 延迟 / 缓冲 / 时间膨胀（Buffering / Time Dilation）

网络不稳定时，客户端加速或扩大缓冲区，减少预测错误带来的卡顿
网络恢复稳定后，缩小缓冲区

4. 命中判定（Hit Registration）

客户端可能预测子弹命中，但服务器是最终权威
远端实体使用插值，服务端回滚远端实体状态判断命中
高延迟情况下可能放弃客户端预测，直接等待服务器确认


设计原则总结


架构为 可维护性与可扩展性服务

ECS + 严格准则（组件无行为，System 少状态）



网络同步：预测 + 回滚 + 丢包/延迟/预测错误补偿


Utility、Singleton、Deferred 操作用于缓解 System 间耦合与副作用



参考资料：
《守望先锋》架构设计和网络同步
]]></content>
      <categories>
        <category>other</category>
      </categories>
      <tags>
        <tag>game dev</tag>
        <tag>architecture</tag>
      </tags>
  </entry>
  <entry>
    <title>一些数学公式</title>
    <url>/other/%E4%B8%80%E4%BA%9B%E6%95%B0%E5%AD%A6%E5%85%AC%E5%BC%8F/</url>
    <content><![CDATA[1. n 阶带拉格朗日余项的泰勒展开式
公式：
f(x)=∑k=0nf(k)(x0)k!(x−x0)k+f(n+1)(ξ)(n+1)!(x−x0)n+1,ξ∈(x0,x)f(x) = \sum_{k=0}^{n} \frac{f^{(k)}(x_0)}{k!} (x-x_0)^k + \frac{f^{(n+1)}(\xi)}{(n+1)!} (x-x_0)^{n+1}, \quad \xi \in (x_0, x)
f(x)=k=0∑n​k!f(k)(x0​)​(x−x0​)k+(n+1)!f(n+1)(ξ)​(x−x0​)n+1,ξ∈(x0​,x)
说明：

前半部分是 n 阶泰勒多项式
后半部分是拉格朗日余项，表示截断误差


2. 欧拉公式
eix=cos⁡x+isin⁡xe^{ix} = \cos x + i \sin x
eix=cosx+isinx
说明：

连接复指数函数与三角函数
是复数分析和信号处理的基础


3. 向量运算基础
内积（点积）：
⟨x,y⟩=x1y1+x2y2+⋯+xnyn\langle x, y \rangle = x_1 y_1 + x_2 y_2 + \cdots + x_n y_n
⟨x,y⟩=x1​y1​+x2​y2​+⋯+xn​yn​
向量模长：
∥x∥=⟨x,x⟩\|x\| = \sqrt{\langle x, x \rangle}
∥x∥=⟨x,x⟩​
向量距离：
d(x,y)=∥x−y∥d(x, y) = \|x - y\|
d(x,y)=∥x−y∥

4. 最小二乘法（Least Squares Method）
目标函数：
12∑i=1n(hθ(x(i))−y(i))2=δ\frac{1}{2} \sum_{i=1}^n \left(h_\theta(x^{(i)}) - y^{(i)}\right)^2 = \delta
21​i=1∑n​(hθ​(x(i))−y(i))2=δ
∑i=1n[hθ(x(i))−y(i)]2=(Xθ−y)T(Xθ−y)\sum_{i=1}^n \left[h_\theta(x^{(i)}) - y^{(i)}\right]^2 = (X\theta - y)^T (X\theta - y)
i=1∑n​[hθ​(x(i))−y(i)]2=(Xθ−y)T(Xθ−y)
求解公式：
hθ(x)=Xθ,y=yh_\theta(x) = X \theta, \quad y = y
hθ​(x)=Xθ,y=y
(Xθ−y)T(Xθ−y)=δ(X\theta - y)^T (X\theta - y) = \delta
(Xθ−y)T(Xθ−y)=δ
2XT(Xθ−y)=02 X^T (X\theta - y) = 0
2XT(Xθ−y)=0
XTXθ=XTyX^T X \theta = X^T y
XTXθ=XTy
θ=(XTX)−1XTy\theta = (X^T X)^{-1} X^T y
θ=(XTX)−1XTy
参考资料：

最小二乘法（least square method）
一文让你彻底搞懂最小二乘法（超详细推导）


]]></content>
      <categories>
        <category>other</category>
      </categories>
      <tags>
        <tag>math</tag>
      </tags>
  </entry>
  <entry>
    <title>图最短路径算法</title>
    <url>/other/%E5%9B%BE%E6%9C%80%E7%9F%AD%E8%B7%AF%E5%BE%84%E7%AE%97%E6%B3%95/</url>
    <content><![CDATA[1. Floyd 算法
功能：计算任意两点间的最短路径，适合稠密图。
参数：

times：二维数组，每个元素 [u, v, w] 表示从 u 到 v 的边权为 w。
n：节点数。
m：起点编号。

返回值：起点 m 到所有点的最远距离，若不可达返回 -1。
代码示例：
int Floyd(vector&lt;vector&lt;int&gt;&gt; &amp;times, int n, int m) &#123;  vector&lt;vector&lt;int&gt;&gt; adjacent_matrix(n, vector&lt;int&gt;(n, INT_MAX / 2));  for (const auto &amp;time : times) &#123;    adjacent_matrix[time[0]][time[1]] = time[2];  &#125;  for (int i = 0; i &lt; n; i++)    for (int j = 0; j &lt; n; j++)      for (int k = 0; k &lt; n; k++)        if (adjacent_matrix[j][i] != INT_MAX / 2 &amp;&amp; adjacent_matrix[i][k] != INT_MAX / 2)          adjacent_matrix[j][k] = min(adjacent_matrix[j][k], adjacent_matrix[j][i] + adjacent_matrix[i][k]);  int max_res = *max_element(adjacent_matrix[m].begin(), adjacent_matrix[m].end());  return max_res == INT_MAX / 2 ? -1 : max_res;&#125;
解析：

核心思想：动态规划，依次尝试每个节点作为中间节点更新最短路径。
优点：能处理任意两点间最短路径，逻辑清晰。
缺点：时间复杂度 O(n3)O(n^3)O(n3)，大图不适合。


2. Dijkstra 算法（优先队列优化）
功能：单源最短路径，适合边权非负稀疏图。
参数：

times：边集合。
n：节点数。
k：起点编号。

返回值：起点 k 到所有点的最远距离，若不可达返回 -1。
代码示例：
int Dijkstra_priority(vector&lt;vector&lt;int&gt;&gt; &amp;times, int n, int k) &#123;  vector&lt;int&gt; he(n, -1), e(times.size()), ne(times.size()), w(times.size());  for (int i = 0; i &lt; times.size(); i++) &#123;    e[i] = times[i][1];    w[i] = times[i][2];    ne[i] = he[times[i][0]];    he[times[i][0]] = i;  &#125;  vector&lt;int&gt; dis(n, INT_MAX / 2);  priority_queue&lt;pair&lt;int, int&gt;, vector&lt;pair&lt;int, int&gt;&gt;, greater&lt;pair&lt;int, int&gt;&gt;&gt; pq;  dis[k] = 0;  pq.push(&#123;0, k&#125;);  vector&lt;bool&gt; visited(n, false);  while (!pq.empty()) &#123;    auto [step, id] = pq.top(); pq.pop();    if (visited[id]) continue;    visited[id] = true;    for (int j = he[id]; j != -1; j = ne[j]) &#123;      int dst = e[j];      if (dis[dst] &gt; dis[id] + w[j]) &#123;        dis[dst] = dis[id] + w[j];        pq.push(&#123;dis[dst], dst&#125;);      &#125;    &#125;  &#125;  int max_res = *max_element(dis.begin(), dis.end());  return max_res == INT_MAX / 2 ? -1 : max_res;&#125;
解析：

核心思想：贪心 + 最小堆，优先扩展距离最短的节点。
优点：时间复杂度 O(Elog⁡V)O(E \log V)O(ElogV)，适合稀疏图。
缺点：无法处理负权边。


3. Bellman-Ford 算法
功能：单源最短路径，可处理负权边。
代码示例：
int Bellman_Ford(vector&lt;vector&lt;int&gt;&gt; &amp;times, int n, int k) &#123;  vector&lt;int&gt; dis(n, INT_MAX / 2);  dis[k] = 0;  bool updated = false;  for (int i = 0; i &lt; n; i++) &#123;    updated = false;    for (auto time : times) &#123;      int u = time[0], v = time[1], w = time[2];      if (dis[u] != INT_MAX / 2 &amp;&amp; dis[u] + w &lt; dis[v]) &#123;        dis[v] = dis[u] + w;        updated = true;      &#125;    &#125;    if (!updated) break;  &#125;  if (updated) return -1; // 负环  int max_res = *max_element(dis.begin(), dis.end());  return max_res == INT_MAX / 2 ? -1 : max_res;&#125;
解析：

核心思想：松弛操作迭代 n-1 次，确保最短路径更新到最优。
优点：可处理负权边，逻辑简单。
缺点：时间复杂度 O(VE)O(VE)O(VE)，图大时慢；检测负环。


4. SPFA（Bellman-Ford 队列优化）
功能：Bellman-Ford 优化版，用队列减少冗余松弛。
代码示例：
int Spfa(vector&lt;vector&lt;int&gt;&gt; &amp;times, int n, int k) &#123;  vector&lt;int&gt; he(n, -1), e(times.size()), ne(times.size()), w(times.size());  for (int i = 0; i &lt; times.size(); i++) &#123;    e[i] = times[i][1];    w[i] = times[i][2];    ne[i] = he[times[i][0]];    he[times[i][0]] = i;  &#125;  vector&lt;int&gt; cnt(n, 0), dis(n, INT_MAX / 2);  vector&lt;bool&gt; visited(n, false);  dis[k] = 0;  queue&lt;int&gt; q;  q.push(k);  visited[k] = true;  while (!q.empty()) &#123;    int i = q.front(); q.pop();    visited[i] = false;    for (int j = he[i]; j != -1; j = ne[j]) &#123;      int dst = e[j];      if (dis[i] != INT_MAX / 2 &amp;&amp; dis[dst] &gt; dis[i] + w[j]) &#123;        dis[dst] = dis[i] + w[j];        if (!visited[dst]) &#123;          q.push(dst);          visited[dst] = true;          if (++cnt[dst] &gt; n) return -1; // 负环        &#125;      &#125;    &#125;  &#125;  int max_res = *max_element(dis.begin(), dis.end());  return max_res == INT_MAX / 2 ? -1 : max_res;&#125;
解析：

核心思想：队列优化，避免无效松弛。
优点：平均复杂度比 Bellman-Ford 更低，适合稀疏图。
缺点：仍无法保证最坏情况性能，仍需检测负环。


方法对比



方法
时间复杂度
适用场景
是否支持负权
备注




Floyd
O(n³)
稠密图
否
任意两点最短路径，简单直观


Dijkstra（优先队列）
O(E log V)
稀疏图，边权非负
否
单源最短路径，高效


Bellman-Ford
O(VE)
可含负权边图
是
能检测负环


SPFA
平均 O(E)
稀疏图，可含负权
是
Bellman-Ford 队列优化版




]]></content>
      <categories>
        <category>other</category>
      </categories>
      <tags>
        <tag>algorithm</tag>
      </tags>
  </entry>
  <entry>
    <title>最速降线曲线</title>
    <url>/other/%E6%9C%80%E9%80%9F%E9%99%8D%E7%BA%BF%E6%9B%B2%E7%BA%BF/</url>
    <content><![CDATA[最速降线（Brachistochrone）
最速降线是指在重力作用下，从点 A 到点 B 物体运动时间最短的曲线。
运动时间公式：
T=∫xAxB1+[y′(x)]22g(yA−y(x)) dxT = \int_{x_A}^{x_B} \frac{\sqrt{1 + [y&#x27;(x)]^2}}{\sqrt{2g(y_A - y(x))}} \, dx
T=∫xA​xB​​2g(yA​−y(x))​1+[y′(x)]2​​dx

泛函与欧拉-拉格朗日方程
泛函定义：
I[y]=∫x1x2f(x,y,y′) dxI[y] = \int_{x_1}^{x_2} f(x, y, y&#x27;) \, dx
I[y]=∫x1​x2​​f(x,y,y′)dx
对泛函进行微扰：
Y=y+ϵδ(x),δ(x1)=δ(x2)=0Y = y + \epsilon \delta(x), \quad \delta(x_1) = \delta(x_2) = 0
Y=y+ϵδ(x),δ(x1​)=δ(x2​)=0
I[y+ϵδ(x)]=∫x1x2f(x,Y,Y′) dxI[y + \epsilon \delta(x)] = \int_{x_1}^{x_2} f(x, Y, Y&#x27;) \, dx
I[y+ϵδ(x)]=∫x1​x2​​f(x,Y,Y′)dx
取极值的必要条件：
ddϵI[y+ϵδ(x)]∣ϵ=0=0\left. \frac{d}{d\epsilon} I[y+\epsilon \delta(x)] \right|_{\epsilon=0} = 0
dϵd​I[y+ϵδ(x)]​ϵ=0​=0
dF(ϵ)dϵ=∫x1x2[∂f∂Y−ddx(∂f∂Y′)]δ(x) dx=0\frac{dF(\epsilon)}{d\epsilon} = \int_{x_1}^{x_2} \left[ \frac{\partial f}{\partial Y} - \frac{d}{dx} \left( \frac{\partial f}{\partial Y&#x27;} \right) \right] \delta(x) \, dx = 0
dϵdF(ϵ)​=∫x1​x2​​[∂Y∂f​−dxd​(∂Y′∂f​)]δ(x)dx=0
对任意 δ(x)\delta(x)δ(x)，得到欧拉-拉格朗日方程：
∂f∂y−ddx(∂f∂y′)=0\frac{\partial f}{\partial y} - \frac{d}{dx} \left( \frac{\partial f}{\partial y&#x27;} \right) = 0
∂y∂f​−dxd​(∂y′∂f​)=0

贝尔特拉米积分（Beltrami Identity）
如果 F(y,y′)F(y, y&#x27;)F(y,y′) 不显含 xxx，满足欧拉-拉格朗日方程：
ddx(∂F∂y′)=∂F∂y\frac{d}{dx} \left( \frac{\partial F}{\partial y&#x27;} \right) = \frac{\partial F}{\partial y}
dxd​(∂y′∂F​)=∂y∂F​
定义：
E=F−y′∂F∂y′E = F - y&#x27; \frac{\partial F}{\partial y&#x27;}
E=F−y′∂y′∂F​
得：
F−y′∂F∂y′=CF - y&#x27; \frac{\partial F}{\partial y&#x27;} = C
F−y′∂y′∂F​=C

最速降线求解
泛函：
L(y,y′)=1+y′22g(yA−y)L(y, y&#x27;) = \frac{\sqrt{1 + y&#x27;^2}}{\sqrt{2g(y_A - y)}}
L(y,y′)=2g(yA​−y)​1+y′2​​
由于不显含 xxx，使用贝尔特拉米积分：
∂L∂y′=y′2g(yA−y)1+y′2\frac{\partial L}{\partial y&#x27;} = \frac{y&#x27;}{\sqrt{2g(y_A - y)} \sqrt{1 + y&#x27;^2}}
∂y′∂L​=2g(yA​−y)​1+y′2​y′​
解得：
1+y′2=12gC2(yA−y)1 + y&#x27;^2 = \frac{1}{2gC^2 (y_A - y)}
1+y′2=2gC2(yA​−y)1​
令 y=yA−uy = y_A - uy=yA​−u：
u12gC2−u du=dx\sqrt{\frac{u}{\frac{1}{2gC^2} - u}} \, du = dx
2gC21​−uu​​du=dx
令 v=2gC2uv = 2gC^2 uv=2gC2u：
dx=12gC2v1−v dvdx = \frac{1}{2gC^2} \sqrt{\frac{v}{1 - v}} \, dv
dx=2gC21​1−vv​​dv
令 v=sin⁡2θv = \sin^2 \thetav=sin2θ 并积分：
x=12gC2∫2sin⁡2θ dθ=12gC2(θ−sin⁡2θ2)+Dx = \frac{1}{2gC^2} \int 2 \sin^2 \theta \, d\theta = \frac{1}{2gC^2} \left( \theta - \frac{\sin 2\theta}{2} \right) + D
x=2gC21​∫2sin2θdθ=2gC21​(θ−2sin2θ​)+D
y=yA−sin⁡2θ2gC2y = y_A - \frac{\sin^2 \theta}{2gC^2}
y=yA​−2gC2sin2θ​
最终可写成摆线方程：
x=a(θ−sin⁡θ)+xAx = a (\theta - \sin \theta) + x_A
x=a(θ−sinθ)+xA​
y=−a(1−cos⁡θ)+yAy = -a (1 - \cos \theta) + y_A
y=−a(1−cosθ)+yA​
a=14gC2a = \frac{1}{4 g C^2}
a=4gC21​
其中 aaa 为滚动圆半径^a。

参考

最速降线的数学模型—变分法 - 简书
摆线的那些事儿——数学界的大型装逼事件 - 知乎

]]></content>
      <categories>
        <category>other</category>
      </categories>
      <tags>
        <tag>math</tag>
      </tags>
  </entry>
  <entry>
    <title>相机视觉基础</title>
    <url>/other/%E7%9B%B8%E6%9C%BA%E8%A7%86%E8%A7%89%E5%9F%BA%E7%A1%80/</url>
    <content><![CDATA[1. 相机参数
1.1 分辨率（Resolution）
指相机一次采集图像的像素点数，也可理解为传感器上感光单元的数量。分辨率越高，图像细节越丰富。

1.2 传感器尺寸（Sensor Size）
决定相机的成像视野大小，常见尺寸有 1/3&quot;、1/2&quot;、2/3&quot;、1&quot; 等。

1.3 像元尺寸（Pixel Size）
单个像素感光单元的尺寸。尺寸越大，感光能力越强，信噪比越高，图像质量更好。
公式：
像元尺寸=传感器尺寸分辨率（像素个数）\text{像元尺寸} = \frac{\text{传感器尺寸}}{\text{分辨率（像素个数）}}
像元尺寸=分辨率（像素个数）传感器尺寸​

1.4 图像精度（Accuracy）
指一个像素代表的实际物理尺寸，常用单位：μm/pixel。
公式：
精度=视野（FOV）分辨率\text{精度} = \frac{\text{视野（FOV）}}{\text{分辨率}}
精度=分辨率视野（FOV）​

背光源应用：推荐精度为 1–3 像素
正光源应用：推荐精度为 3–5 像素


1.5 ISO
表示图像传感器对光的灵敏度。

实际上 ISO 调整的是光电信号的放大倍数，并非传感器本身的灵敏度。
ISO 过高会引入噪点，降低图像质量。


2. 镜头参数
2.1 焦距（Focal Length, f）
焦距决定成像比例与视场范围。

物距 &gt; 2f：像距在 f–2f 之间，成 倒立缩小实像（照相机成像）。
f &lt; 物距 &lt; 2f：像距 &gt; 2f，成 倒立放大实像（幻灯机、微距拍摄）。
物距 &lt; f：像距为负，在物体同侧成 正立放大虚像（放大镜）。

定焦镜头的对焦环实质上调整的是像距。
.fxufyvjehivv{}

2.2 视场角（Field of View, FOV）
镜头可覆盖的最大成像范围，取决于 镜头焦距 与 传感器尺寸。
.uebwlrcgftua{}

2.3 光圈（Aperture, f-number, F）
光圈大小用 F 值 表示：
F=fDF = \frac{f}{D}
F=Df​
其中 f 为焦距，D 为入瞳直径。

F 值越小 → 光圈越大 → 进光量越多。
F 值越大 → 光圈越小 → 进光量越少。


2.4 景深（Depth of Field, DOF）
景深表示在一定清晰度范围内，被摄体前后可接受的成像范围。

本质：只有一个平面真正处于合焦状态，其他位置形成“弥散圆”（Circle of Confusion）。
影响因素：

光圈大小：景深与光圈近似 成反比（光圈越大，景深越小）。
焦距：景深与 焦距平方成反比（焦距越长，景深越小，影响比光圈更显著）。
物距：景深与 物距平方成正比（物体越远，景深越大）。



.xjtnxplsprle{}
.koiqhxmttork{}
.uxjfnswyuieh{}

2.5 工作距离（Working Distance, WD）
指镜头最前端到被摄物体的距离。工作距离过小可能导致安装受限。
.iqoqsvhiczrn{}

3. 参考资料

工业相机与镜头选型方法（含实例）
镜头焦距、光圈、物距与景深之间的关系
什么是视场角？它的大小由什么决定？
工业相机各种参数计算方法

]]></content>
      <categories>
        <category>other</category>
      </categories>
      <tags>
        <tag>cv</tag>
      </tags>
  </entry>
</search>
