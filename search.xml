<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Hello World err</title>
    <url>/hello-world/</url>
    <content><![CDATA[Welcome to Hexo! This is your very first post. Check documentation for more info. If you get any problems when using Hexo, you can find the answer in troubleshooting or you can ask me on GitHub.
Quick Start
Create a new post
$ hexo new &quot;My New Post&quot;
More info: Writing
Run server
$ hexo server
More info: Server
Generate static files
$ hexo generate
More info: Generating
Deploy to remote sites
$ hexo deploy
More info: Deployment
]]></content>
  </entry>
  <entry>
    <title>Unity协程</title>
    <url>/gameframework/unity%E5%8D%8F%E7%A8%8B/</url>
    <content><![CDATA[Unity 协程 vs Update轮询 笔记
1. 本质概念

Update轮询：每一帧主动检测状态（例：请求完成了吗？动画播完了吗？）。
协程 Coroutine：逻辑暂停，等待条件满足时，Unity自动帮你恢复执行。

2. 核心区别对比



对比项
Update轮询
协程




检查方式
每帧自己 if 检查
yield return 后自动等待


CPU开销
多对象轮询，累积大
统一协程队列，调度轻


写法复杂度
高，状态机复杂
低，顺序逻辑清晰


可读性
乱，容易出错
像同步代码，自然直观


出错概率
高，容易漏判、重复判
低，写完就能跑




一句话总结：协程让&quot;等待&quot;自动化，而不用自己傻傻地每帧查。


3. 使用 Update 检测请求（传统写法）
csharp复制编辑private UnityWebRequest m_Request;public void StartRequest(string url)&#123;    m_Request = UnityWebRequest.Get(url);    m_Request.SendWebRequest();&#125;private void Update()&#123;    if (m_Request == null || !m_Request.isDone)        return;    if (m_Request.result == UnityWebRequest.Result.ConnectionError || m_Request.result == UnityWebRequest.Result.ProtocolError)    &#123;        Debug.LogError(&quot;Request Error: &quot; + m_Request.error);    &#125;    else    &#123;        Debug.Log(&quot;Request Success: &quot; + m_Request.downloadHandler.text);    &#125;    m_Request = null; // 清空，避免重复处理&#125;

缺点：Update里增加负担，需要手动管理状态。


4. 使用协程检测请求（推荐写法）
csharp复制编辑public void StartRequest(string url)&#123;    StartCoroutine(RequestCoroutine(url));&#125;private IEnumerator RequestCoroutine(string url)&#123;    using (UnityWebRequest request = UnityWebRequest.Get(url))    &#123;        yield return request.SendWebRequest();        if (request.result == UnityWebRequest.Result.ConnectionError || request.result == UnityWebRequest.Result.ProtocolError)        &#123;            Debug.LogError(&quot;Request Error: &quot; + request.error);        &#125;        else        &#123;            Debug.Log(&quot;Request Success: &quot; + request.downloadHandler.text);        &#125;    &#125;&#125;

优点：自动挂起等待，不用管完成时机，写法直观，逻辑自然。


5. 协程背后的调度机制（理解协程为什么高效）

yield return 后，协程对象会被挂起。
Unity引擎集中管理所有挂起协程。
每帧检查协程状态量非常小（而不是 N 个对象各自检查）。
完成时直接从挂起队列里唤醒并继续执行。

]]></content>
      <categories>
        <category>gameframework</category>
      </categories>
      <tags>
        <tag>c#</tag>
        <tag>unity</tag>
      </tags>
  </entry>
  <entry>
    <title>细节实现</title>
    <url>/gameframework/%E7%BB%86%E8%8A%82%E5%AE%9E%E7%8E%B0/</url>
    <content><![CDATA[ReferencePool
IReference
被引用的对象需实现IReference接口（包括一个Clear函数），当对象被回收时调用，恢复初始状态，以便下次使用。
ReferenceCollection
代表一种类型的引用池，位于ReferencePool内部
private readonly Queue&lt;IReference&gt; m_References;private readonly Type m_ReferenceType;...//状态计数
ReferenceInfo
获取引用池的状态计数，相较ReferenceCollection少了m_References
ReferencePool
静态类，管理所有类型的引用池
private static readonly Dictionary&lt;Type, ReferenceCollection&gt; s_ReferenceCollections = new Dictionary&lt;Type, ReferenceCollection&gt;();public static T Acquire&lt;T&gt;() where T : class, IReference, new() =&gt; GetReferenceCollection(typeof(T)).Acquire&lt;T&gt;();public static IReference Acquire(Type referenceType)&#123;    InternalCheckReferenceType(referenceType);    return GetReferenceCollection(referenceType).Acquire();&#125;
具体操作时先根据类型（为Type时先要对类型进行检查）获取对应的ReferenceCollection，再进行其他操作。
Fsm
FsmState&lt; T &gt;
状态机状态的抽象类，包括状态初始化，状态更新等等
IFsm&lt; T &gt;
状态机的接口
FsmBase
状态机的抽象类
Fsm&lt; T &gt;
internal sealed class Fsm&lt;T&gt; : FsmBase, IReference, IFsm&lt;T&gt; where T : class&#123;    private T m_Owner;    private readonly Dictionary&lt;Type, FsmState&lt;T&gt;&gt; m_States;    private Dictionary&lt;string, Variable&gt; m_Datas;    private FsmState&lt;T&gt; m_CurrentState;
一个状态机，包括所有状态类以及当前数据
IFsmManager
接口，管理所有状态机
Procedure
ProcedureManager
特殊的Fsm，也交给FsmManager管理
Setting
只需根据ISettingHelper创建设置辅助器即可完成，ISettingManager用于使用
module
只暴露接口，具体实现在只在程序集可见，使用getmodule进行实例化
public static bool InScene(this GameObject gameObject)
this GameObject gameObject：

this：表示这是一个扩展方法，并且该方法是对 GameObject 类型的扩展。
GameObject：表示要扩展的类型（这里是 Unity 的 GameObject 类）。
gameObject：是方法的参数，表示调用该方法时传入的目标对象。

DataProvider
ReadData
m_ResourceManager.LoadAsset加载资产，并把一个回调函数传进去，回调函数激发provider的事件

private void OnCloseUIFormComplete(object sender, CloseUIFormCompleteEventArgs e)&#123;    m_EventComponent.Fire(this, CloseUIFormCompleteEventArgs.Create(e));&#125;
​                m_UIManager.CloseUIFormComplete += OnCloseUIFormComplete;
在gameframework提供接口，在runtime里面将不同模块结合起来
]]></content>
      <categories>
        <category>gameframework</category>
      </categories>
      <tags>
        <tag>c#</tag>
        <tag>unity</tag>
      </tags>
  </entry>
  <entry>
    <title>缓存系统</title>
    <url>/leveldb/cache/</url>
    <content><![CDATA[缓存系统
基于 LRUCache 的缓存系统，其结构包括哈希表和 LRU 双向链表。
LRUHandle

哈希表相关: next_hash 用于链接哈希表中的条目。
链表相关: next 和 prev 用于在 LRU 链表和使用中的链表（in_use）中形成双向链表。

哈希表


结构: 哈希表由多个存储桶组成，每个存储桶包含一个链接链表，用于存储散列到该存储桶中的缓存条目。


插入逻辑：

先找到对应的存储桶。
比较哈希值和键值，如果找到则替换条目，否则将新条目插入到链表末尾。
当条目数量超过容量时，重新调整哈希表的大小。



LRUCache
由哈希表、LRU 链表头和 in_use 链表头构成。

插入过程:

加锁。
根据键创建 handle，引用计数加 1，并插入到 in_use_ 链表。
将 handle 添加到哈希表中。
如果键已存在，则返回旧的 handle，并从缓存和链表中删除旧条目，引用计数减 1。
当缓存容量超出限制且 LRU 链表不为空时，从 LRU 链表中移除第一个条目，并从缓存和链表中删除。


查找过程:

加锁。
在哈希表中查找 handle 并返回，引用计数加 1。


释放过程:

使用完 handle 后需要手动释放。
加锁，引用计数减 1。


擦除过程:

加锁，从缓存中移除条目，引用计数减 1。



SharedLRUCache
对 LRUCache 进行进一步封装，通过取哈希值的高 4 位来决定分片位置。

分片优势: 通过分片将缓存操作分散到多个 LRUCache 实例中，提升了访问效率，降低了锁的竞争，改善了并发性能。

]]></content>
      <categories>
        <category>leveldb</category>
      </categories>
      <tags>
        <tag>c++</tag>
        <tag>dataset</tag>
      </tags>
  </entry>
  <entry>
    <title>迭代器</title>
    <url>/leveldb/dbiter/</url>
    <content><![CDATA[Iterator* DBImpl::NewIterator(const ReadOptions&amp; options) &#123;  SequenceNumber latest_snapshot;  uint32_t seed;  Iterator* iter = NewInternalIterator(options, &amp;latest_snapshot, &amp;seed);  return NewDBIterator(this, user_comparator(), iter,                       (options.snapshot != nullptr                            ? static_cast&lt;const SnapshotImpl*&gt;(options.snapshot)                                  -&gt;sequence_number()                            : latest_snapshot),                       seed);&#125;Iterator* DBImpl::NewInternalIterator(const ReadOptions&amp; options,                                      SequenceNumber* latest_snapshot,                                      uint32_t* seed) &#123;  mutex_.Lock();  *latest_snapshot = versions_-&gt;LastSequence();  // Collect together all needed child iterators  std::vector&lt;Iterator*&gt; list;  list.push_back(mem_-&gt;NewIterator());  mem_-&gt;Ref();  if (imm_ != nullptr) &#123;    list.push_back(imm_-&gt;NewIterator());    imm_-&gt;Ref();  &#125;  versions_-&gt;current()-&gt;AddIterators(options, &amp;list);  Iterator* internal_iter = NewMergingIterator(&amp;internal_comparator_, &amp;list[0],                                               static_cast&lt;int&gt;(list.size()));  versions_-&gt;current()-&gt;Ref();  IterState* cleanup = new IterState(&amp;mutex_, mem_, imm_, versions_-&gt;current());  internal_iter-&gt;RegisterCleanup(CleanupIteratorState, cleanup, nullptr);  *seed = ++seed_;  mutex_.Unlock();  return internal_iter;&#125;
具体代码解释如下
void DBIter::Next() &#123;  assert(valid_);  if (direction_ == kReverse) &#123;  // Switch directions?    direction_ = kForward;    // iter_ is pointing just before the entries for this-&gt;key(),    // so advance into the range of entries for this-&gt;key() and then    // use the normal skipping code below.    if (!iter_-&gt;Valid()) &#123;      iter_-&gt;SeekToFirst();    &#125; else &#123;      iter_-&gt;Next();    &#125;    if (!iter_-&gt;Valid()) &#123;      valid_ = false;      saved_key_.clear();      return;    &#125;    // saved_key_ already contains the key to skip past.  &#125; else &#123;    // Store in saved_key_ the current key so we skip it below.    SaveKey(ExtractUserKey(iter_-&gt;key()), &amp;saved_key_);    // iter_ is pointing to current key. We can now safely move to the next to    // avoid checking current key.    iter_-&gt;Next();    if (!iter_-&gt;Valid()) &#123;      valid_ = false;      saved_key_.clear();      return;    &#125;  &#125;  FindNextUserEntry(true, &amp;saved_key_);&#125;void DBIter::FindNextUserEntry(bool skipping, std::string* skip) &#123;  // Loop until we hit an acceptable entry to yield  assert(iter_-&gt;Valid());  assert(direction_ == kForward);  do &#123;    ParsedInternalKey ikey;	    if (ParseKey(&amp;ikey) &amp;&amp; ikey.sequence &lt;= sequence_) &#123;	// 根据当前iter_的key封装成一个新ikey，同时要求ikey的序列号小于创建DBIter时传入的序列号      switch (ikey.type) &#123;        case kTypeDeletion:          // Arrange to skip all upcoming entries for this key since          // they are hidden by this deletion.          SaveKey(ikey.user_key, skip);		          skipping = true;		// 如果当前ikey的类型是删除，将跳过整个 user_key = ikey.user_key()的nodes          break;        case kTypeValue:          if (skipping &amp;&amp;	// 跳过              user_comparator_-&gt;Compare(ikey.user_key, *skip) &lt;= 0) &#123;	// 旧条目不再需要            // Entry hidden          &#125; else &#123;	// 否则就是本 user_key 所在nodes中的最新节点            valid_ = true;            saved_key_.clear();            return;          &#125;          break;      &#125;    &#125;    iter_-&gt;Next();	// 定位到下一个  &#125; while (iter_-&gt;Valid());  saved_key_.clear();  valid_ = false;&#125;
1. DBIter::Next()


作用：
实现迭代器前进到下一个用户可见的条目。


核心逻辑：


断言当前状态有效：
assert(valid_); 确保调用 Next() 时，迭代器处于有效状态。


处理不同方向：


如果当前迭代方向为 kReverse，说明之前的操作是反向遍历：
切换方向为 kForward。因为反向遍历时迭代器指向当前 key 之前的位置，所以需要先移动一步使其进入当前 key 的区间：若当前迭代器无效，则调用 SeekToFirst() 重置到首元素。否则调用 iter_-&gt;Next()。若移动后迭代器变为无效，则标记迭代器为无效并清空 saved_key_，返回。此时，saved_key_ 内保存了需要跳过的 key。


当方向已经是kForward时：
首先将当前 key 提取后保存到 saved_key_，用于后续跳过同一用户键的所有条目（防止重复返回）。调用 iter_-&gt;Next() 移动到下一个内部条目。同样，若移动后无效，则更新状态并返回。




寻找下一个用户条目：
最后调用 FindNextUserEntry(true, &amp;saved_key_)，从当前位置继续寻找一个用户可见的有效条目。





2. DBIter::FindNextUserEntry(bool skipping, std::string* skip)


作用：
在内部迭代器当前位置开始，寻找下一个用户层面上“有效”（即未被删除、未被隐藏）的条目。


核心逻辑：


循环遍历：
循环直到找到一个符合条件的用户条目，或者迭代器到达末尾。


解析内部键：
每次循环调用 ParseKey(&amp;ikey) 解析当前内部键 iter_-&gt;key()，同时检查 ikey.sequence 是否在允许的序列号范围内（&lt;= sequence_）。


根据操作类型处理：


删除类型（kTypeDeletion）：
将当前 ikey.user_key 保存到 skip（即 saved_key_），并设置 skipping 为 true，表示后续相同用户键的条目应当被跳过（删除操作“屏蔽”了旧值）。


值类型（kTypeValue）：
如果处于跳过状态（skipping 为 true），并且当前 ikey.user_key 与 *skip 比较小于等于（即还处于需要跳过的范围内），则认为该条目被删除或隐藏，不予返回。否则，找到了一个有效的用户条目，将 valid_ 标记为 true，清空 saved_key_（因为找到有效数据后，不需要再跳过）。返回，结束循环。




结束条件：

如果循环结束时 iter_-&gt;Valid() 为 false，则表示已经遍历完所有条目，
清空 saved_key_，并将 valid_ 标记为 false。





]]></content>
      <categories>
        <category>leveldb</category>
      </categories>
      <tags>
        <tag>c++</tag>
        <tag>dataset</tag>
      </tags>
  </entry>
  <entry>
    <title>日志</title>
    <url>/leveldb/log/</url>
    <content><![CDATA[日志结构
.bmenfztkovkd{zoom:67%;}

static const int kBlockSize = 32768;
// Header is checksum (4 bytes), length (2 bytes), type (1 byte).
static const int kHeaderSize = 4 + 2 + 1

日志写入流程

初始化写入器
调用 Next 函数获取 singleWriter，用于处理单条日志记录的写入。
分块（Chunk）管理

开始首个 Chunk：singleWriter 初始化后，启动第一个 Chunk 的写入。
数据缓冲：将日志记录数据按顺序写入内存缓冲区。
缓冲区阈值检测：持续监测缓冲区大小，当数据部分（不含 Header）达到 32 KiB - 7 字节（预留 7 字节 Header 空间）时，触发分块操作。


构建并写入 Chunk

生成 Header：为当前 Chunk 计算校验和，并确定类型（如跨块时的 First/Middle，或结尾的 Last）。
完整 Chunk 写入：将 Header（7 字节） + 数据（≤32 KiB -7 字节） 组合成完整 Chunk，一次性写入日志文件。
重置缓冲区：清空当前缓冲区，准备接收后续数据。


循环处理与结束标记

重复上述过程，直至日志记录所有数据写入完成。
最后一个 Chunk 标记为 Last 类型，表示记录结束。若数据恰好填满 Chunk，则前一个 Chunk 标记为 Last。




日志读取流程

按块读取数据
每次从日志文件读取 32 KiB 的 Block，作为基础处理单元。
初始化读取器
调用 Next 函数获取 singleReader，用于逐条解析日志记录。
解析 Chunk

读取 Header：从 Block 头部提取 校验和、数据长度、类型（如 Full/First/Middle/Last）。
数据提取：根据 Header 中的数据长度，从 Block 中读取对应字节的数据内容。


校验与错误处理

校验和验证：计算数据部分的校验和，与 Header 中的值比对。
错误处理：

校验失败：若用户启用严格模式，返回错误；否则丢弃当前 Chunk，继续读取后续 Block。
校验成功：保留数据，准备拼接。




数据拼接与终止条件

中间 Chunk：若类型为 First 或 Middle，将数据暂存，继续读取下一 Block。
终止 Chunk：遇到 Last 类型时，将当前 Chunk 数据与前序数据合并，返回完整日志记录。



]]></content>
      <categories>
        <category>leveldb</category>
      </categories>
      <tags>
        <tag>c++</tag>
        <tag>dataset</tag>
      </tags>
  </entry>
  <entry>
    <title>内存数据库</title>
    <url>/leveldb/memtable/</url>
    <content><![CDATA[
内存数据库
leveldb内存数据库基于跳表结构。
跳表
template &lt;typename Key, class Comparator&gt;class SkipList &#123;    private:    struct Node;&#125;template &lt;typename Key, class Comparator&gt;struct SkipList&lt;Key, Comparator&gt;::Node &#123;    explicit Node(const Key&amp; k) : key(k) &#123;&#125;        const Key key;        Node* Next(int n) &#123;        assert(n &gt;= 0);        return next_[n].load(std::memory_order_acquire);    &#125;        void SetNext(int n, Node* x) &#123;        assert(n &gt;= 0);        next_[n].store(x, std::memory_order_release);    &#125;        Node* NoBarrier_Next(int n) &#123;        assert(n &gt;= 0);        return next_[n].load(std::memory_order_relaxed);    &#125;        void NoBarrier_SetNext(int n, Node* x) &#123;        assert(n &gt;= 0);        next_[n].store(x, std::memory_order_relaxed);    &#125;private:    std::atomic&lt;Node*&gt; next_[1];&#125;;
memory_order 说明

memory_order_relaxed: 只确保操作是原子性的，不对内存顺序做任何保证。
memory_order_release: 用于写操作，例如 std::atomic::store(T, memory_order_release)，会在写操作之前插入一个 StoreStore屏障，确保屏障之前的所有操作不会重排到屏障之后。
memory_order_acquire: 用于读操作，例如 std::atomic::load(memory_order_acquire)，会在读操作之后插入一个 LoadLoad 屏障，确保屏障之后的所有操作不会重排到屏障之前。
memory_order_acq_rel: 等效于 memory_order_acquire 和 memory_order_release 的结合，同时插入一个 StoreStore 屏障与 LoadLoad 屏障，用于读写操作，例如 std::atomic::fetch_add(T, memory_order_acq_rel)。

示例
// 线程 A (产生者)node-&gt;data = 42;  // 初始化数据list.SetNext(0, node);  // 通过 memory_order_release 让数据对其他线程可见// 线程 B (消费者)Node* node = list.Next(0);  // 使用 memory_order_acquire 获取节点int value = node-&gt;data;  // 确保读取的是线程 A 完全初始化后的数据
编译器优化：编译器可能会认为，node-&gt;data = 42 和 list.SetNext(0, node) 没有直接依赖关系。因此，它可能将 list.SetNext(0, node) 提前。编译器这种优化是合法的，因为在单线程语境下，它认为不会改变行为。
模板例子
template &lt;typename T&gt;class Example &#123;public:    struct NestedType &#123;&#125;;     // 嵌套类型    static int value;         // 静态非类型成员&#125;;template &lt;typename T&gt;void Func() &#123;    typename Example&lt;T&gt;::NestedType* ptr;  // 嵌套类型    Example&lt;T&gt;::value = 42;       // 静态成员&#125;// 对于 Example&lt;T&gt;::NestedType，编译器可能无法区分它是一个类型还是非类型成员（如变量、函数等）。// 在模板类或函数中，如果某个嵌套成员依赖于模板参数，编译器无法在模板实体化之前确定它的具体含义。~~~c++template &lt;typename Key, class Comparator&gt;typename SkipList&lt;Key, Comparator&gt;::Node* SkipList&lt;Key, Comparator&gt;::NewNode(    const Key&amp; key, int height) &#123;  char* const node_memory = arena_-&gt;AllocateAligned(      sizeof(Node) +      sizeof(std::atomic&lt;Node*&gt;) * (height - 1));  // 动态内存分配Node*  return new (node_memory) Node(key);&#125;//t柔性数组和定制内存分配，优化了跳表的内存效率、访问速度
跳表的查找

根据跳表高度选取最高层的头节点；
若小于，取该层的下一个节点；
若等于，则直接返回；
若大于且层高不为0，降低层高继续查找。

跳表的插入

查找过程中，不断记录每层的前任节点；
为新节点随机生成层高，并根据前任节点信息，将节点插入到层的链表中；

graph TD    subgraph MemTable_System [&quot;MemTable 系统&quot;]        MemTable --&gt; SkipList        MemTable --&gt; Arena        SkipList --&gt; Entry        Entry --&gt; Arena        SkipList --&gt; KeyComparator    end
.miepshkesgxu{zoom:50%;}]]></content>
      <categories>
        <category>leveldb</category>
      </categories>
      <tags>
        <tag>c++</tag>
        <tag>dataset</tag>
      </tags>
  </entry>
  <entry>
    <title>读写过程</title>
    <url>/leveldb/read-write/</url>
    <content><![CDATA[写入过程
Leveldb的put/delete操作实际都被转成batch然后write。
WriteBatch batch;batch.Put(kvp.first, kvp.second);EXPECT_TRUE(db_-&gt;Write(WriteOptions(), &amp;batch).ok());
在leveldb中，使用Write操作进行写入，流程如下：

创建writer对象并加入队列（同时只允许一个写入操作运行）

Status DBImpl::Write(const WriteOptions&amp; options, WriteBatch* updates) &#123;  Writer w(&amp;mutex_);  w.batch = updates;  w.sync = options.sync;  w.done = false;  MutexLock l(&amp;mutex_);  writers_.push_back(&amp;w);  // 若当前 Writer 不是队列头部，则等待（锁被释放，进入阻塞）  while (!w.done &amp;&amp; &amp;w != writers_.front()) &#123;    w.cv.Wait();  &#125;  if (w.done) &#123;    return w.status;  &#125;  ...&#125;

为写操作分配空间

Status status = MakeRoomForWrite(updates == nullptr);uint64_t last_sequence = versions_-&gt;LastSequence();Writer* last_writer = &amp;w;

构建批次并执行写操作

if (status.ok() &amp;&amp; updates != nullptr) &#123;  WriteBatch* write_batch = BuildBatchGroup(&amp;last_writer);  WriteBatchInternal::SetSequence(write_batch, last_sequence + 1);  last_sequence += WriteBatchInternal::Count(write_batch);  &#123;    mutex_.Unlock();    status = log_-&gt;AddRecord(WriteBatchInternal::Contents(write_batch));    bool sync_error = false;    if (status.ok() &amp;&amp; options.sync) &#123;      status = logfile_-&gt;Sync();      if (!status.ok()) &#123;        sync_error = true;      &#125;    &#125;    if (status.ok()) &#123;      status = WriteBatchInternal::InsertInto(write_batch, mem_);    &#125;    mutex_.Lock();    if (sync_error) &#123;      RecordBackgroundError(status);    &#125;  &#125;  versions_-&gt;SetLastSequence(last_sequence);&#125;

清理并唤醒写操作

while (true) &#123;  Writer* ready = writers_.front();  writers_.pop_front();  if (ready != &amp;w) &#123;    ready-&gt;status = status;    ready-&gt;done = true;    ready-&gt;cv.Signal();  &#125;  if (ready == last_writer) break;&#125;if (!writers_.empty()) &#123;  writers_.front()-&gt;cv.Signal();&#125;
读取过程

加锁，基于snapshot进行数据读取

Status DBImpl::Get(const ReadOptions&amp; options, const Slice&amp; key,                   std::string* value) &#123;  Status s;  MutexLock l(&amp;mutex_);  SequenceNumber snapshot;  if (options.snapshot != nullptr) &#123;    snapshot =        static_cast&lt;const SnapshotImpl*&gt;(options.snapshot)-&gt;sequence_number();  &#125; else &#123;    snapshot = versions_-&gt;LastSequence();  &#125;    ......&#125;

引用mem、imm以及current version，避免读取过程中被销毁

MemTable* mem = mem_;  MemTable* imm = imm_;  Version* current = versions_-&gt;current();  mem-&gt;Ref();  if (imm != nullptr) imm-&gt;Ref();  current-&gt;Ref();

查询数据，释放锁避免占用cpu时间过长

// Unlock while reading from files and memtables  &#123;    mutex_.Unlock();    // First look in the memtable, then in the immutable memtable (if any).    LookupKey lkey(key, snapshot);    if (mem-&gt;Get(lkey, value, &amp;s)) &#123;      // Done    &#125; else if (imm != nullptr &amp;&amp; imm-&gt;Get(lkey, value, &amp;s)) &#123;      // Done    &#125; else &#123;      s = current-&gt;Get(options, lkey, value, &amp;stats);      have_stat_update = true;    &#125;    mutex_.Lock();  &#125;

压缩解引等操作

if (have_stat_update &amp;&amp; current-&gt;UpdateStats(stats)) &#123;    MaybeScheduleCompaction();  &#125;  mem-&gt;Unref();  if (imm != nullptr) imm-&gt;Unref();  current-&gt;Unref();  return s;

leveldb读取分为三步：
在memory db中查找指定的key，若搜索到符合条件的数据项，结束查找；
在冻结的memory db中查找指定的key，若搜索到符合条件的数据项，结束查找；
按低层至高层的顺序在level i层的sstable文件中查找指定的key，若搜索到符合条件的数据项，结束查找，否则返回Not Found错误，表示数据库中不存在指定的数据；
注解
注意leveldb在每一层sstable中查找数据时，都是按序依次查找sstable的。
0层的文件比较特殊。由于0层的文件中可能存在key重合的情况，因此在0层中，文件编号大的sstable优先查找。理由是文件编号较大的sstable中存储的总是最新的数据。
非0层文件，一层中所有文件之间的key不重合，因此leveldb可以借助sstable的元数据（一个文件中最小与最大的key值）进行快速定位，每一层只需要查找一个sstable文件的内容。

]]></content>
      <categories>
        <category>leveldb</category>
      </categories>
      <tags>
        <tag>c++</tag>
        <tag>dataset</tag>
      </tags>
  </entry>
  <entry>
    <title>排序字符串表</title>
    <url>/leveldb/sstable/</url>
    <content><![CDATA[
SSTable
物理结构
SSTable 按块划分，默认数据块大小为 4KiB。每个数据块除了存储数据外，还包括两个辅助字段：压缩类型和 CRC 校验码。
逻辑结构


Data Block：存储键值对。
Filter Block：存储过滤器相关数据，采用专门的 FilterBlock。
Meta Index Block：存储 Filter Block 在 SSTable 文件中的位置和长度。
Index Block：存储每个 Data Block 的索引信息。
Footer：存储 Meta Index Block 和 Index Block 的索引信息。

读写过程
写过程

读过程

SSTable 使用两种缓存机制：

Table Cache：存储已打开的 SSTable 文件。
Block Cache：存储已读取的数据块。

Table Cache 的结构如下：

Table 的迭代器
TwoLevelIterator
TwoLevelIterator 用于高效处理两层索引结构。它结合索引块和数据块，提供了一种高效的访问机制。
using BlockFunction = Iterator* (*)(void*, const ReadOptions&amp;, const Slice&amp;);explicit TwoLevelIterator(Iterator* index_iter, BlockFunction block_function,                          void* arg, const ReadOptions&amp; options);//使用 arg 而不是直接传递 Table* 主要是为了设计的灵活性和接口的一致性。
实现思路

索引块迭代器：遍历索引块的迭代器，用来获取每个数据块的元信息。
数据块读取函数：提供读取数据块的函数，该函数由 TwoLevelIterator 调用以加载具体的数据块。
封装 TwoLevelIterator：结合索引块迭代器和数据块读取函数，创建 TwoLevelIterator。


]]></content>
      <categories>
        <category>leveldb</category>
      </categories>
      <tags>
        <tag>c++</tag>
        <tag>dataset</tag>
      </tags>
  </entry>
  <entry>
    <title>Pimpl</title>
    <url>/other/Pimpl/</url>
    <content><![CDATA[Pimpl
// my_class.hclass my_class &#123;   //  ... all public and protected stuff goes here ...private:   class impl; unique_ptr&lt;impl&gt; pimpl; // opaque type here&#125;;// my_class.cppclass my_class::impl &#123;  // defined privately here  // ... all private data and functions: all of these  //     can now change without recompiling callers ...&#125;;my_class::my_class(): pimpl( new impl )&#123;  // ... set impl values ...&#125;


减少编译依赖


分离接口和实现


可移植性

以虚函数作为接口在二进制兼容性方面有本质困难：“一旦发布，不能修改”。



]]></content>
      <categories>
        <category>other</category>
      </categories>
      <tags>
        <tag>c++</tag>
        <tag>design pattern</tag>
      </tags>
  </entry>
  <entry>
    <title>整体架构</title>
    <url>/leveldb/%E6%95%B4%E4%BD%93%E6%9E%B6%E6%9E%84/</url>
    <content><![CDATA[Leveldb 整体架构
Leveldb 是一个高性能的键值存储库，由 Google 开发。它的设计目标是提供快速的读写操作，同时支持高效的压缩和持久化。以下是 Leveldb的核心组件及其工作原理的详细说明：

1. MemTable
概述

MemTable 是 Leveldb 在内存中用于组织和维护数据的结构。
写入操作不会直接刷新到磁盘文件，而是先写入 MemTable，因此写入性能非常高。

特点


基于 SkipList 实现，支持高效的插入、删除和查找操作。


数据按键排序存储，便于后续的持久化操作。


详情

详情见 内存数据库。


2. Immutable MemTable
概述

当 MemTable 的大小达到预设阈值时，它会转换为 Immutable MemTable。
Immutable MemTable 是不可修改的，只允许读取操作。

作用

后台压缩进程会将 Immutable MemTable 中的数据转换为 SSTable，并持久化到磁盘中。
这种设计避免了写入阻塞，同时保证了数据的持久化。


3. Log (Write-Ahead Log, WAL)
概述

Leveldb 的写入操作首先会顺序写入日志文件（Log），然后再写入 MemTable。
日志文件用于在系统崩溃时恢复未持久化的数据。

特点

日志文件是追加写入的，因此写入性能非常高。
日志文件与 MemTable 的数据保持一致，确保数据的可靠性。

详情

详情见 日志。


4. SSTable (Sorted String Table)
概述

当内存中的数据超过一定阈值时，Leveldb会将数据持久化到磁盘，形成 SSTable 文件。
SSTable 是 Leveldb在磁盘上存储数据的主要形式。

层级结构

SSTable 文件在逻辑上分为多个层级（Level），最底层为 Level 0，由内存直接持久化生成。
更高层级的 SSTable 文件通过 Compaction 操作整合生成，以减少文件之间的交集。

详情

详情见 排序字符串表。


5. Manifest
概述

Manifest 文件记录了 Leveldb的元数据信息，包括 SSTable 文件的元信息、版本变更等。

核心组件

FileMetaData：记录 SSTable 文件的元信息，包括文件编号、文件大小、最小键、最大键等。
VersionEdit：记录数据库版本的变更，可能包含新增的文件元信息、删除的文件等。

工作原理

Manifest 文件与 Write-Ahead Log (WAL) 使用相同的存储方式。
每次数据库启动时，都会通过 Manifest 文件恢复最新的版本信息。

详情

详情见。


6. 读写操作
概述

Leveldb的读写操作是其核心功能，设计目标是高效、可靠。

写操作

数据首先写入日志文件（Log），然后写入 MemTable。
当 MemTable 达到阈值时，转换为 Immutable MemTable，并触发后台压缩。

读操作

数据首先从 MemTable 和 Immutable MemTable 中查找。
如果未找到，则从磁盘中的 SSTable 文件中查找。

详情

详情见 读写过程。


7. 缓存系统
概述

Leveldb使用缓存系统来加速读取操作，减少磁盘 I/O。

核心组件

Block Cache：缓存 SSTable 文件中的数据块，加速读取操作。
Table Cache：缓存 SSTable 文件的元信息，减少文件打开和关闭的开销。

详情

详情见 缓存系统。


8. Compaction
概述

Compaction 是 Leveldb 的核心机制之一，用于整合和优化 SSTable 文件。

作用

减少 SSTable 文件之间的交集，提高读取效率。
清理过期或删除的数据，释放磁盘空间。

类型

Minor Compaction：将 Immutable MemTable 转换为 SSTable 文件。
Major Compaction：整合多个 SSTable 文件，生成更高层级的文件。

详情

详情见 。


总结
Leveldb的整体架构通过 MemTable、Immutable MemTable、Log、SSTable、Manifest 等组件的协同工作，实现了高效的读写操作和持久化存储。其设计充分考虑了性能、可靠性和可扩展性，适用于多种应用场景。
]]></content>
      <categories>
        <category>leveldb</category>
      </categories>
      <tags>
        <tag>c++</tag>
        <tag>dataset</tag>
      </tags>
  </entry>
  <entry>
    <title>Spring</title>
    <url>/other/Spring/</url>
    <content><![CDATA[
一、Spring XML 配置与注解
1.1 beans.xml 中的自动扫描配置
&lt;context:component-scan base-package=&quot;com.example&quot;/&gt;


作用：Spring 会扫描指定包及其子包下的类，如果类上标有如下注解：

@Component
@Service
@Repository
@Controller

则会将其注册为 Spring 管理的 Bean。



1.2 常见注解解释



注解
说明




@Component
标注普通组件，交给 Spring 容器管理


@Service
业务逻辑层组件，语义化更清晰


@Repository
DAO 层组件，支持异常转换


@Controller
控制层组件，处理 Web 请求


@Autowired
按类型自动注入依赖


@Qualifier(&quot;beanName&quot;)
与 @Autowired 配合，按名称注入


@PostConstruct
初始化方法（容器加载后执行）


@PreDestroy
销毁方法（容器关闭前执行）


@Scope(&quot;singleton&quot;)
单例（默认）


@Scope(&quot;prototype&quot;)
多例，每次获取新对象




二、Spring AOP 与 AspectJ 区别
2.1 Spring AOP

方式：基于 JDK 或 CGLIB 动态代理
原理：在运行时为目标对象生成代理类，拦截方法调用，插入增强逻辑
优点：非侵入式，简单易用
缺点：运行时生成代理，存在性能开销

2.2 AspectJ

方式：

编译时织入（Compile-Time Weaving）
类加载时织入（Load-Time Weaving）


原理：修改字节码，将切面代码插入目标类
优点：性能更高，控制更精细
缺点：配置复杂，侵入性强


三、项目结构分层（经典三层架构）
controller --&gt; service --&gt; dao     |            |          | Web层     业务逻辑层   数据访问层

POJO（Plain Old Java Object）：表示数据结构的简单 Java 类，常用于 entity 或 pojo 包中。
控制器 (Controller)：接收请求、返回响应；
服务层 (Service)：封装具体业务逻辑；
DAO 层 (Dao)：对数据库进行 CRUD 操作。


四、MySQL 数据库设计范式（Normalization）
✅ 第一范式（1NF）——字段原子性

每列必须是不可再拆分的最小数据单位。
✅ 示例：姓名 字段不能为 &quot;张三,李四&quot;，应拆成多条记录。


✅ 第二范式（2NF）——消除部分依赖

非主键字段必须完全依赖于主键，不能依赖于主键的一部分（适用于复合主键）。
✅ 示例：主键是 学生ID+课程ID，若 学生姓名 只依赖 学生ID，应将其拆分成新表。


✅ 第三范式（3NF）——消除传递依赖

非主键字段不能依赖于另一个非主键字段。
✅ 示例：班级编号 → 班主任 → 电话号，此时电话号依赖于班主任，应拆表。


🔚 总结

Spring 支持注解与 XML 配置并用，提高了开发灵活性。
Spring AOP 简单易用适合常规开发；AspectJ 适合对性能有更高要求的场景。
三层架构利于职责分离、项目维护。
数据库设计中应遵循范式，避免冗余和异常依赖关系。

]]></content>
      <categories>
        <category>other</category>
      </categories>
      <tags>
        <tag>java</tag>
        <tag>spring</tag>
      </tags>
  </entry>
  <entry>
    <title>Algorithm</title>
    <url>/other/algorithm/</url>
    <content><![CDATA[为了提升软件编码能力，小王制定了刷题计划，他选了题库中的n道题，编号从0到n-1，并计划在m天内按照题目编号顺序刷完所有的题目（注意，小王不能用多天完成同一题）。
在小王刷题计划中，小王需要用time[i]的时间完成编号 i 的题目。
此外，小王还可以查看答案，可以省去该题的做题时间。为了真正达到刷题效果，小王每天最多直接看一次答案。
我们定义m天中做题时间最多的一天耗时为T（直接看答案的题目不计入做题总时间)。
请你帮小王求出最小的T是多少。

注意到求解限制较多，而验证比较方便，可以考虑二分法再进行验证

]]></content>
      <categories>
        <category>other</category>
      </categories>
      <tags>
        <tag>algorithm</tag>
      </tags>
  </entry>
  <entry>
    <title>aysnc</title>
    <url>/other/async/</url>
    <content><![CDATA[static async Task Main(string[] args)        &#123;            // 同步倒咖啡            Coffee cup = PourCoffee();            Console.WriteLine(&quot;coffee is ready&quot;);            // 并发开始煎蛋、炸薯饼、烤吐司            var eggsTask = FryEggsAsync(2);            var hashBrownTask = FryHashBrownsAsync(3);            var toastTask = MakeToastWithButterAndJamAsync(2);            var breakfastTasks = new List&lt;Task&gt; &#123; eggsTask, hashBrownTask, toastTask &#125;;            while (breakfastTasks.Count &gt; 0)            &#123;                // 等待任意一个任务完成                Task finishedTask = await Task.WhenAny(breakfastTasks);                if (finishedTask == eggsTask)                    Console.WriteLine(&quot;eggs are ready&quot;);                else if (finishedTask == hashBrownTask)                    Console.WriteLine(&quot;hash browns are ready&quot;);                else if (finishedTask == toastTask)                    Console.WriteLine(&quot;toast is ready&quot;);                // 可选：处理异常                await finishedTask;                // 从任务列表移除                breakfastTasks.Remove(finishedTask);            &#125;            // 同步倒橙汁            Juice oj = PourOJ();            Console.WriteLine(&quot;oj is ready&quot;);            Console.WriteLine(&quot;Breakfast is ready!&quot;);        &#125;
🔹 I/O Bound（CPU 用于等待）


解释：程序主要瓶颈在等待外部资源返回结果，CPU 实际上大部分时间都在空等。


做法：
✅ 使用 await 等待异步操作结果，释放线程资源，避免阻塞主线程或线程池。


await 会：

挂起当前 UI 线程方法（比如 Button 点击回调）。
把控制权交还给 UI 消息循环，不会阻塞界面。
等到请求返回结果后，再切回 UI 线程继续执行。

示例：
var result = await HttpClient.GetAsync(&quot;https://api.example.com&quot;);


官方文档参考：
Async in depth | Microsoft Docs



🔸 CPU Bound（CPU 用于计算）


解释：程序瓶颈在本地 CPU 运算，I/O 操作极少，计算密集。


做法：
✅ 使用 Task.Run(() =&gt; &#123;...&#125;) 将任务放到线程池，并行执行，减轻主线程负担。
示例：
var result = await Task.Run(() =&gt; CalculateSomethingHeavy());


官方文档参考：
同样是 Async in depth | Microsoft Docs


]]></content>
      <categories>
        <category>other</category>
      </categories>
      <tags>
        <tag>c#</tag>
        <tag>async</tag>
      </tags>
  </entry>
  <entry>
    <title>git开发流程</title>
    <url>/other/git/</url>
    <content><![CDATA[Git 协作流程核心摘要
1. 各分支核心职责 (Core Responsibilities of Each Branch)



分支类型
生命周期
核心职责




master
永久
只读。永远指向最新已发布版本的代码快照。是生产环境代码的最终来源。


release/x.y.z
长期，随版本存在
开发、测试、修复的主战场。所有面向该版本的 feature 和 hotfix 都合并到这里。新版本从上一个 release 分支创建。


feature/xxx
短期，随任务生灭
开发新功能。必须从其目标 release 分支创建，并最终合并回该 release 分支。


hotfix/xxx
短期，紧急修复用
修复线上紧急 Bug。从出问题的 release 分支创建，修复后合并回该分支，并必须同步到所有后续的 release 分支中。



2. 个人开发标准流程 (Standard Workflow for Individual Developers)
这是一个开发者从接到任务到提交 PR 的完整步骤。
第一步：同步并创建分支
从你任务对应的 目标 release 分支 拉取最新代码，并创建你的功能分支。
# 1. 切换到目标 release 分支（例如 1.11.0）git checkout release/1.11.0# 2. 拉取最新代码，确保本地是最新状态git pull origin release/1.11.0# 3. 创建你的功能分支git checkout -b feature/your-task-name
第二步：本地开发与提交
在你的 feature 分支上进行编码，并进行多次原子化的提交。
git add .git commit -m &quot;feat: implement user login modal&quot;git add .git commit -m &quot;fix: resolve button style issue&quot;
第三步：整理提交记录 (提交 PR 前的关键一步)
在发起 PR 前，使用交互式 rebase (rebase -i) 来清理你的提交历史，将多个零碎的 commit 合并成一个或几个有意义的 commit。
# 1. 拉取远程 release 分支的最新变动，以防冲突git fetch origin# 2. 以远程 release 分支为基准，进行交互式 rebasegit rebase -i origin/release/1.11.0
第四步：推送并创建 PR
由于 rebase 修改了 git 历史，你需要强制推送到你的远程分支。
# -f 是 --force 的缩写，强制推送git push -f origin feature/your-task-name
最后，在 GitHub / GitLab 等平台上，从你的 feature/your-task-name 分支向目标 release/1.11.0 分支发起 Pull Request。
3. PR 审查与合并流程 (PR Review and Merge Process)
这是项目维护者 (Maintainer) 或团队负责人收到 PR 后的操作流程。
第一步：代码审查 (Code Review)

逻辑审查：检查代码的业务逻辑、健壮性和潜在风险。
规范审查：检查代码风格、命名是否符合团队规范。
目标分支确认：确保 PR 提交的目标分支是正确的 release 分支。
至少需要一名或多名成员 Approve。

第二步：合并前检查

CI/CD 状态：确保所有自动化检查（如测试、构建）都已通过。
提交历史：检查 PR 的 commit 记录是否已按要求整理干净。如果未整理，要求提交者返回上一步（个人流程的第三步）进行 rebase。
合并冲突：确保 PR 没有合并冲突。如有冲突，要求提交者在本地解决冲突后再提交。

第三步：执行合并 (推荐在 Git 平台 UI 上操作)
强烈推荐使用以下两种合并策略，严禁使用 Merge Commit。



合并方式
操作方式
优点




Squash and Merge (推荐)
在平台点击 “Squash and Merge” 按钮。
将 PR 内所有 commit 合并成一个，生成一个非常干净的 release 分支历史。


Rebase and Merge (可选)
在平台点击 “Rebase and Merge” 按钮。
将 PR 内已整理好的 commit 按顺序追加到 release 分支，保持线性历史。



第四步：清理分支
PR 合并后，应立即删除已被合并的 feature 分支，保持仓库整洁。

在 Git 平台上，通常在合并后会有**“Delete branch”**的按钮，直接点击即可。
如果需要手动删除远程分支，可执行：# 删除远程的 feature 分支git push origin --delete feature/your-task-name

]]></content>
      <categories>
        <category>other</category>
      </categories>
      <tags>
        <tag>tool</tag>
      </tags>
  </entry>
  <entry>
    <title>双重抽象</title>
    <url>/other/%E5%8F%8C%E9%87%8D%E6%8A%BD%E8%B1%A1/</url>
    <content><![CDATA[双重抽象
这个设计模式的核心思想是 “双重抽象”，通过抽象类与泛型接口的配合，实现了对象池系统的 类型通用性与类型安全性分离。以下是关键点概括：

设计意图总结
分工明确的双抽象层

ObjectPoolBase（非泛型抽象类）

提供类型无关的通用操作（如池名称、对象计数、容量管理等）
允许以统一方式（如ObjectPoolBase[]）处理不同类型对象池的集合操作


IObjectPool&lt;T&gt;（泛型接口）

提供类型安全的对象操作（如Spawn&lt;T&gt;、Register&lt;T&gt;）
确保编译时类型检查，避免运行时类型错误





应用场景分离

需要处理多类型池集合时
（如全局管理、编辑器界面）→ 使用 ObjectPoolBase
例：统计所有池的基础信息、批量释放资源
需要操作具体类型对象时
（如业务逻辑调用）→ 使用 IObjectPool&lt;T&gt;
例：从特定池中获取UI实例或游戏实体



实现类ObjectPool的作用
作为二者具体实现，同时满足：

通过基类支持跨类型统一管理
通过接口提供类型安全操作




类比说明



维度
ObjectPoolBase（抽象类）
IObjectPool&lt;T&gt;（泛型接口）




类型处理
忽略具体类型，通用操作
强类型约束，安全操作


典型使用场景
管理器、编辑器工具等跨类型场景
业务逻辑如UIManager、EntityGroup


设计目的
提供&quot;集合视角&quot;的统一入口
提供&quot;具体类型视角&quot;的安全操作




为何不合并？

若仅用泛型接口：无法以统一类型（如数组）管理不同T的池实例。
若仅用非泛型基类：失去类型安全，需频繁类型转换，增加运行时风险。

此设计通过双重抽象，在类型擦除（集合操作）和类型保留（具体操作）间取得平衡，是框架设计中典型的&quot;分离关注点&quot;实践。
]]></content>
      <categories>
        <category>other</category>
      </categories>
      <tags>
        <tag>c#</tag>
        <tag>design pattern</tag>
      </tags>
  </entry>
  <entry>
    <title>一些数学公式</title>
    <url>/other/%E4%B8%80%E4%BA%9B%E6%95%B0%E5%AD%A6%E5%85%AC%E5%BC%8F/</url>
    <content><![CDATA[n阶带拉格朗日余项的泰勒展开式
f(x)=∑k=0∞f(k)(x0)k!(x−x0)k+f(n+1)(ξ)(n+1)!(x−x0)n+1f(x)=\sum_{k=0}^{\infin}{f^{(k)}(x_0)\over k!}(x-x_0)^k+{f^{(n+1)}(\xi)\over (n+1)!}(x-x_0)^{n+1}
f(x)=k=0∑∞​k!f(k)(x0​)​(x−x0​)k+(n+1)!f(n+1)(ξ)​(x−x0​)n+1
欧拉公式
eix=cosx+isinxe^{ix}=cosx+isinx
eix=cosx+isinx
向量运算基础
⟨x,y⟩=x1⋅y1+x2⋅y2+⋯+xn⋅yn\langle x,y \rangle = x_1 \cdot y_1 + x_2 \cdot y_2 + \cdots + x_n \cdot y_n
⟨x,y⟩=x1​⋅y1​+x2​⋅y2​+⋯+xn​⋅yn​
∥x∥=⟨x,x⟩\|x\| = \sqrt{\langle x,x \rangle}
∥x∥=⟨x,x⟩​
d(x,y)=∥x−y∥d(x,y) = \|x-y\|
d(x,y)=∥x−y∥
最小二乘法
12∑i=1n(hθ(x(i))−y(i))2=δ\frac{1}{2} \sum_{i=1}^n \left(h_\theta(x^{(i)}) - y^{(i)}\right)^2 = \delta
21​i=1∑n​(hθ​(x(i))−y(i))2=δ
∑i=1n[hθ(x(i))−y(i)]2=[hθ(x)−y]T[hθ(x)−y]\sum_{i=1}^n \left[h_\theta(x^{(i)}) - y^{(i)}\right]^2 = \left[h_\theta(x) - y\right]^T \left[h_\theta(x) - y\right]
i=1∑n​[hθ​(x(i))−y(i)]2=[hθ​(x)−y]T[hθ​(x)−y]
hθ(x)=Xθ,y=yh_\theta(x) = X\theta, \quad y = y
hθ​(x)=Xθ,y=y
(Xθ−y)T(Xθ−y)=δ(X\theta - y)^T (X\theta - y) = \delta
(Xθ−y)T(Xθ−y)=δ
2XT(Xθ−y)=02X^T (X\theta - y) = 0
2XT(Xθ−y)=0
XTXθ=XTyX^T X \theta = X^T y
XTXθ=XTy
θ=(XTX)−1XTy\theta = (X^T X)^{-1} X^T y
θ=(XTX)−1XTy
https://zhuanlan.zhihu.com/p/38128785
https://blog.csdn.net/MoreAction_/article/details/106443383
矩阵求导公式

]]></content>
      <categories>
        <category>other</category>
      </categories>
      <tags>
        <tag>math</tag>
      </tags>
  </entry>
  <entry>
    <title>视觉</title>
    <url>/other/%E8%A7%86%E8%A7%89/</url>
    <content><![CDATA[1.相机
1.1 分辨率：相机每次采集图像的像素点数，也就是相机有多少个感光晶片。
1.2 传感器尺寸：影响视野大小

1.3 像元尺寸：尺寸越大，感光面积越大，图片质量也会越好

像元尺寸=传感器尺寸/分辨率（像元个数）

1.4 图像精度：精度指一个像素表示实际物体的大小，用（um*um）/pixel表示。精度值越小，精度越高。


精度值=视野/分辨率


使用背光源的精度为1~3个像素，使用正光源的精度为3~5个像素


1.5 ISO

传感器并不会随着ISO的提升而变得敏感，真正改变的是光电信号的放大程度
ISO过大会出现很大噪点

2.镜头
2.1 焦距（focal length）

当物距在无穷远和两倍焦距之间时，像距在焦距和两倍焦距之间，成倒立缩小的实像（照相机）。
当物距小于两倍焦距大于焦距时，像距大于两倍焦距，成倒立放大的实像（幻灯机和部分支持微距拍摄的照相机）。
当物距小于焦距时，像距为负，在物体的同侧成正立放大的虚像（放大镜）。
定焦镜头的对焦环调整的是像距


2.2 视场角（Field of view, 即FOV）
视野指的是镜头能看到的最大范围，也就是镜头所能覆盖的有效工作区域。只受镜头的焦距和图像传感器的尺寸影响。

2.3 光圈（Iris）
光圈大小一般用F表示，以镜头焦距f和通光孔径直径D的比值来衡量，当光圈物理孔径不变时，镜头中心与感光器件距离越远，F值越大，光圈越小；反之，F值越小，光圈越大。
2.4 景深（Depth of view，即DOF）


理解景深原理前，我们必须明白一件事：当我们对焦时，其实只有一个平面是真正合焦的。这个平面与像平面（可以简单理解为胶片或者传感器平面）平行。凡是在这个平面之前或者之后的都不是合焦状态。合焦平面上物体某点发出不同角度的光在像平面成像都汇聚于一点，而非合焦物体的某点发出不同角度的光会落在像平面不同点上，形成一个模糊圆，这个圆术语叫做弥散圆（circle of confusion）。


景深与光圈近似成反比关系，即光圈越大，景深越小（光圈越大，F值越小，进光量越多，有效孔径越大，景深越小）。
景深与焦距f的平方近似成反比关系。即焦距越长，景深越小。而且由于平方关系，所以焦距f对景深的影响比光圈N更大；
景深与被摄物距离U的平方近似成正比关系。即物距越大，景深越大。同样，由于平方关系，景物距离U对景深的影响也比光圈更大。

2.5 工作距离（Working Distance）
工作距离指的是镜头的最下端到景物之间的距离。

参考：

工业相机与镜头选型方法（含实例）_工业相机选型-CSDN博客
镜头焦距、光圈、物距与景深之间的关系_透镜焦距与景深的关系公式-CSDN博客
什么是视场角？它的大小是由镜头还是传感器决定的？
工业相机各种参数计算方法

]]></content>
      <categories>
        <category>other</category>
      </categories>
      <tags>
        <tag>vision</tag>
      </tags>
  </entry>
</search>
